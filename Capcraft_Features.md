# Capcraft v1.2 상세 기능 명세서

## 1. 캡처 기능 (Capture Modes)
- **전체 화면 캡처**: 시스템 전체 화면을 즉시 캡처하여 캔버스로 불러옵니다. (단축키: `F2`)
- **부분 화면 캡처 (직접 지정)**: 마우스를 드래그하여 원하는 영역만 캡처합니다. 캡처 시 가로/세로 십자 가이드라인(Crosshair)이 표시되어 정밀한 선택이 가능합니다. (단축키: `F3`)

## 2. 캔버스 및 개체 조작 (Canvas & Object Manipulation)
- **이동 및 크기 조절**: 추가된 모든 개체(도형, 텍스트, 이미지 등)는 마우스로 자유롭게 이동 및 크기 조절이 가능합니다.
- **회전 기능**: 개체 선택 시 나타나는 모서리 조절점 중 **우측 상단 모서리(회전 아이콘)**를 드래그하여 개체를 회전할 수 있습니다. (회전 시 개체의 **좌측 상단 꼭지점**을 축으로 회전합니다.)
- **반전 기능**: 개체 선택 시 상단에 표시되는 제어 버튼을 통해 '좌우 반전' 및 '상하 반전'을 즉시 적용할 수 있습니다.
- **레이어(Z-index) 제어**:
  - 개체 선택 시 상단의 '맨 뒤로' 및 '맨 앞으로' 버튼을 사용하여 개체의 겹침 순서를 제어합니다.
  - 캔버스에서 개체를 클릭하면 활성화되면서 자동으로 맨 앞으로 표시됩니다.
- **제어 버튼 편의성 및 배치**: 개체 상단에 표시되는 제어 버튼들은 `좌우대칭`, `상하대칭`, `맨뒤로`, `맨앞으로`, `글상자로` 순서로 일관되게 배치되며, 버튼의 끝부분에만 마우스가 닿아도 쉽게 클릭되도록 판정 영역(Hit box)이 대폭 확장(cornerSize 100, touchCornerSize 100)되어 있습니다.
- **이미지류 개체(이모티콘, 이미지, 모자이크) 제어 버튼**: 이모티콘, 이미지, 모자이크를 삽입하고 선택할 때에도 '좌우반전', '상하반전', '맨 뒤로', '맨 앞으로' 버튼이 모두 제공되어 자유롭게 제어할 수 있습니다. 단, 이 개체들에는 '글상자로' 버튼이 표시되지 않습니다.

## 3. 그리기 및 도형 도구 (Drawing & Shapes)
- **펜 도구 (볼펜)**: 자유 곡선을 그릴 수 있으며 기본적으로 굵기 3, 빨간색으로 설정됩니다.
- **형광펜 도구**: 반투명한 선을 그릴 수 있으며 굵기, 색상, 투명도(Opacity)를 조절할 수 있습니다.
- **도형 도구**:
  - 직사각형 (선 색상, 선 굵기, 배경색, 점선 적용 가능)
  - 마름모 (선 색상, 선 굵기, 배경색, 점선 적용 가능)
  - 타원 (선 색상, 선 굵기, 배경색, 점선 적용 가능)
  - 선분 (선 색상, 굵기, 점선 적용 가능)
  - 화살표 (선 색상, 굵기, 점선 적용 가능)
- **도형 내 글상자 통합 (`글상자로`)**: 삽입된 직사각형, 마름모, 타원을 선택하고 `글상자로` 버튼을 누르면 도형 내부에 텍스트를 바로 입력할 수 있습니다.
  - 이미 텍스트가 삽입된 도형이 선택되지 않은 상태에서 한 번 클릭하면 그냥 그 도형만 인식하고(도형 크기 조절 및 회전 가능하도록), 그 도형이 선택된 상태에서 도형 내부의 텍스트 영역을 한 번 더 클릭하면 마우스로 클릭한 정확한 위치에서 커서가 깜빡이며 텍스트를 바로 수정할 수 있습니다. 이때 상단바의 도구 버튼이 활성화되지 않은 상태라도 텍스트 속성 조절 토글 패널이 자동으로 나타나 편집을 돕고, 바깥을 클릭해 편집을 종료하면 패널이 사라집니다.
  - 상단 패널에 나타나는 텍스트 속성 (크기, 굵기, 기울임, 밑줄, **좌/중/우 정렬**, 글자색, 배경색) 설정 패널이 열리며, 이 기본값이 도형 내 텍스트에 적용됩니다. 개별 개체가 아닌 도형의 일부로 귀속됩니다. 따라서 텍스트만 단독으로 선택하여 이동시킬 수 없으며, 도형을 움직이면 텍스트도 완벽하게 함께 이동합니다.
  - 입력된 텍스트가 도형의 너비에 도달하면 자동으로 줄바꿈(Word Wrap)이 적용되어 텍스트가 도형 밖으로 삐져나가지 않고 내부에 머물게 됩니다. 특히 마름모와 타원은 모서리의 각도 및 곡률을 고려하여 텍스트 상자가 외곽선이 아닌 도형 내부에 완벽히 위치하도록 엄격하게 계산됩니다. 텍스트 입력량(줄 수)에 따라 도형의 크기가 자동으로 확대되며, 텍스트를 지우면 처음 생성된 크기까지 자동으로 축소됩니다.
  - 글상자가 적용된 도형(직사각형, 마름모, 타원)의 크기를 사용자가 임의로 줄일 때, 내부의 텍스트가 삐져나오는 것을 막기 위해 텍스트 입력창 영역을 벗어나게 축소할 수 없도록 강하게 제한됩니다. 즉, 도형의 최소 크기는 내부 텍스트의 크기에 비례하여 한계가 정해집니다. 이때 내부의 텍스트 자체 폰트 크기는 변하지 않고 형태만 연동됩니다.
  - 삽입된 텍스트는 도형과 개별적으로 분리되어 움직이지 않습니다. 도형을 움직이면 텍스트도 완전히 일체화되어 함께 이동합니다. 텍스트를 직접 드래그하려 해도 텍스트는 독립적으로 선택되지 않고 배경 도형만 인식됩니다. 
  - 텍스트를 수정하려면 도형이 선택(활성화)된 상태에서 텍스트 영역을 한 번 더 클릭하면 됩니다. 이때 마우스 클릭과 동시에 커서가 깜빡거리며 편집 상태로 진입하고 상단에 텍스트 속성 패널이 열립니다.
  - 도형의 모서리를 잡고 회전(Rotate)시키거나, **회전된 상태에서 마우스로 드래그하여 이동**시킬 때도 시각적인 지연이나 분리 현상 없이 내부 텍스트가 도형 정중앙에 완벽하게 고정되어 실시간으로 일체화되어 함께 회전하고 움직입니다.
  - '글상자로' 버튼 클릭 시, 일반 텍스트 입력 도구와 동일하게 상단에 텍스트 속성 패널(크기, 색상, B/I/U 등)이 자동으로 열리며, 삽입되는 텍스트는 패널에 지정된 기본 설정값을 그대로 따릅니다.

## 4. 텍스트 및 이모지 도구 (Text & Emoji)
- **텍스트 삽입 (Text)**: 
  - 클릭한 위치에 즉시 커서가 활성화되어 바로 텍스트를 입력할 수 있습니다. 처음 텍스트를 입력할 때는 **엔터키를 누르기 전까지 줄바꿈 없이 가로로 계속 늘어나며** 입력됩니다.
  - 상단 패널의 속성 조절: **크기, 굵게(B), 기울임(I), 밑줄(U), 정렬(왼쪽/가운데/오른쪽), 텍스트 색상, 텍스트 배경색** 지정 가능. 편집 모드 중 텍스트의 특정 부분을 마우스로 드래그하여 블록(영역) 지정한 후 상단 패널 설정을 변경하면, 전체 텍스트가 아닌 해당 블록에만 선택적으로 크기나 스타일(B/I/U/색상 등)이 적용됩니다. 정렬은 전체 객체에 적용됩니다. (도형이 글상자로 된 경우도 동일하게 적용됩니다.)
  - 텍스트 입력창 위로 마우스를 올리면 마우스 포인터가 텍스트 편집이 가능함을 직관적으로 알 수 있도록 **대문자 I 모양(텍스트 커서)**으로 변경됩니다. 상단바의 도구가 모두 비활성화된 상태에서 기존 텍스트를 한 번 클릭하면 그냥 그 창만 인식하고(텍스트 크기 조절 및 회전 가능하도록 외곽선 표시), 그 텍스트가 선택된 상태에서 한 번 더 클릭하면 마우스로 클릭한 정확한 위치에 커서가 깜빡이며 편집 모드로 진입합니다. 이때 상단바의 "텍스트" 도구 버튼은 활성화되지 않지만 텍스트 설정 토글 패널이 나타나 속성을 변경할 수 있으며, 입력창 밖을 클릭해 편집을 마치면 토글 패널이 사라집니다.
  - 사용자가 **각 변(ml, mr)이나 꼭지점(tl, bl, br)을 마우스로 잡아 조절하면 그에 맞춰 입력창의 가로 폭이 고정**되어 텍스트가 줄바꿈됩니다. 이때 모서리나 변을 잡아 조절하더라도 텍스트의 크기(폰트 사이즈)는 절대 변하지 않고 오직 너비만 조절됩니다. 모서리에 마우스를 올릴 때 다른 도형처럼 직관적인 양방향 화살표(크기 조절 커서)가 표시됩니다.
  - 텍스트 입력창의 우측 상단 모서리(tr)에도 다른 개체들과 마찬가지로 직관적인 동그란 조절점 아이콘이 배치되어 있습니다. 이 꼭짓점을 잡아서 드래그하면 텍스트 크기의 확대나 축소 없이 왼쪽 위 꼭짓점을 기준으로 직관적인 회전이 가능합니다. 이 때 회전이 가능함을 나타내는 고유의 흰색 작은 회전형 모양 화살표 커서가 표시됩니다.
  - **텍스트 도구 활성화 중 클릭 보정**: 상단바의 "텍스트" 도구가 켜져 있거나 핀으로 고정되어 있는 상태일지라도, 이미 만들어진 텍스트 개체를 마우스로 클릭하면 그 위에 새로운 텍스트가 중복 생성되지 않고 기존 텍스트 개체를 정상적으로 인식하여 선택/수정할 수 있도록 접근성을 대폭 개선했습니다.
- **이모지 삽입 (Emoji)**: 원하는 이모지를 선택하여 클릭한 위치에 텍스트 형태로 삽입합니다.
  - 기본 내장된 9개의 카테고리 외에 사용자가 **직접 추가할 수 있는 5개의 커스텀 이모지 탭**이 존재합니다.
  - 사용자가 컴퓨터 내의 특정 폴더를 이 커스텀 탭에 연결하면, 해당 폴더 안의 이미지 파일(PNG, JPG, SVG 등)들이 이모지 패널에 즉각적으로 불러와집니다. 이 파일들을 클릭하여 캔버스에 자유롭게 이모지처럼 배치할 수 있습니다.

## 5. 이미지 도구 (Image Tools)
- **이미지 불러오기**: 내 PC의 이미지 파일(PNG, JPG 등)을 캔버스에 삽입할 수 있습니다.
- **자르기 (Crop)**: 캔버스에 삽입된 이미지나 배경 이미지의 특정 부분을 잘라내어 캔버스 크기를 축소하거나 불필요한 편집된 부분을 제거할 수 있습니다.
- **원본 삽입**: 캡처한 원본 배경 이미지를 개체 형태로 캔버스 위에 다시 한 번 삽입합니다.

## 6. 클립보드 및 실행 취소 (Clipboard & History)
- **복사 및 붙여넣기**: 선택한 개체들을 복사(Ctrl+C)하고 붙여넣기(Ctrl+V) 할 수 있습니다.
- **실행 취소(Undo) / 다시 실행(Redo)**: 캔버스에서 일어난 작업(추가, 이동, 삭제 등)의 내역을 기록하여 이전 상태로 되돌리거나 다시 복구할 수 있습니다.

## 7. 커스텀 색상 팔레트 (Color Palette)
총 42종의 색상으로 구성된 하단 팔레트 팝업을 제공합니다. 
- 기본 무채색 및 다채로운 톤의 색상 지원.
- **오피스 색상 7종**: 채도가 높고 선명한 원색 계열 7종 (빨강, 주황, 노랑, 연두, 청록, 파랑, 보라) 포함.
- **형광 색상 7종**: 반투명하게 칠해지는 형광색 계열 7종 포함.

## 8. 환경설정 및 데이터 유지 (Settings Persistence)
- **설정 영구 저장**: 사용자가 '환경설정'에서 변경한 모든 사항(펜, 도형 굵기/색상, 테마 등)은 컴퓨터 시스템에 자동 저장됩니다.
- **사용자 정의 이모티콘 폴더**: 사용자가 이모지 도구에서 지정한 로컬 이모티콘 폴더 경로들도 영구적으로 저장되어, 프로그램을 재실행하거나 컴퓨터를 재부팅해도 그대로 유지됩니다.

## 9. 프로그램 실행 및 UI 크기 (Application Startup & UI Scale)
- **반응형 윈도우 크기**: 프로그램 실행 시 메인 모니터 해상도를 감지하여, 전체 화면의 70% 크기로 윈도우가 열리도록 자동 조정됩니다.
- **UI 배율 자동 조정**: 프로그램 윈도우의 가로 크기에 비례하여 상단바, 사이드바 등 내부 UI 요소들의 확대/축소 배율(Zoom Factor)이 자동으로 조정되어 적절한 크기를 유지합니다.

## 10. 추가 고급 기능 (Advanced Features)
- **자르기(Crop) 즉시 렌더링 동기화**: 캔버스의 이미지를 자른 직후 배경 교체 및 치수 변경 시 DOM 요소 컨테이너 사이즈를 즉각 동기화하고 리렌더링을 강제하여, 즉시 잘린 상태가 반영되도록 처리합니다.
- **고해상도 벡터 렌더링 (Retina Scaling)**: 캔버스의 Retina Scaling 옵션을 활성화하여, 텍스트나 도형 등을 무한히 확대하거나 회전시켜도 픽셀 깨짐 없이 선명한 벡터 화질을 유지합니다.
- **SVG 이모티콘 벡터화 지원**: 파일 확장자가 .svg인 이모티콘을 캔버스에 삽입할 때, 비트맵으로 렌더링하지 않고 벡터 패스(fabric.loadSVGFromURL)로 처리하여 무손실 고화질을 지원합니다.
- **SVG 파일 저장 포맷 지원**: 작업한 결과물을 수학적 벡터 데이터가 살아있는 순수 .svg 포맷으로 내보낼 수 있습니다. 텍스트와 벡터 도형의 무손실 품질 저장이 가능합니다.
- **도구 일회성 사용 및 패널 유지 (One-shot Tool Logic)**: 텍스트, 펜, 도형, 이모티콘, 이미지, 모자이크 도구는 캔버스에 개체를 한 번 삽입(완료)하면 상단바의 툴 버튼이 자동으로 비활성화되어 기본 선택(Cursor) 모드로 돌아갑니다. 단, 하위 옵션 패널(토글창)은 닫히지 않고 그대로 유지됩니다.
- **하위 패널 클릭을 통한 도구 즉시 재활성화**: 상단바 버튼이 비활성화되었더라도 열려 있는 하위 옵션 패널 내부의 속성(예: 도형의 타원, 이모티콘 등)을 클릭하거나 변경하면 상단바의 툴 버튼이 다시 자동으로 켜지며 해당 도구가 재활성화됩니다.
- **도구 고정 핀 (Tool Pin)**: 상단 6개의 도구 버튼 우측 상단에 위치한 '핀(📌)' 아이콘을 클릭하여 활성화(파란색 표시)해두면, 개체를 여러 번 연속해서 삽입해도 해당 도구가 자동으로 비활성화되지 않습니다. 연속 그리기 작업 시 매우 유용하며, 핀 고정 중이라도 버튼 본체를 직접 다시 클릭하거나 다른 도구를 선택하면 도구가 강제 해제됩니다.

## 10. 성능 최적화 및 안정화 (Optimizations & Stability)
- **선분/화살표 도형 비활성화 버그 수정**: '도형' 도구에서 '선분' 또는 '화살표'를 하나 삽입한 뒤에도 상단바의 "도형" 버튼이 비활성화되지 않던 버그를 수정했습니다. 선분/화살표는 다중 클릭(multiClick) 방식으로 그려지며 `finalizeMultiClickDrawing()` 함수에서 완료 처리되는데, 이 함수에 `deactivateActiveTool()` 호출이 누락되어 있었습니다.
- **프로그램 창 깜빡임(Flickering) 현상 해결**: PyQt6/QWebEngine 환경에서 프로그램 창(특히 하단)이 빠르게 깜빡이던 현상을 해결했습니다. 근본 원인은 Chromium 엔진의 GPU 래스터화가 Fabric.js 캔버스의 빈번한 부분 갱신과 충돌하는 것이었습니다. `main.py`에서 `QTWEBENGINE_CHROMIUM_FLAGS` 환경변수에 `--disable-gpu-rasterization --disable-partial-raster` 플래그를 추가하여 GPU 래스터화만 선택적으로 비활성화하고, GPU 컴포지팅(레이어 합성)은 유지함으로써 깜빡임 방지와 렌더링 성능을 동시에 확보했습니다. 추가로 CSS에 `backface-visibility: hidden` 및 `contain: paint`를 적용하여 불필요한 레이어 재합성을 억제했습니다.
- **개체 삽입/이동 시 떨림(Jitter) 현상 해결**: 개체를 삽입하거나 드래그할 때 미세하게 떨리던 현상을 해결했습니다. 원인과 수정 사항:
  1. `enableRetinaScaling: true` 설정이 시스템의 DPI 스케일링(`SetProcessDpiAwareness`, `setZoomFactor`)과 충돌하여 매 프레임 좌표 계산에 서브픽셀 오차가 발생했습니다. → `enableRetinaScaling: false`로 변경하여 DPI 충돌을 제거했습니다.
  2. 도형/선분/화살표 드래그 중 `mouse:move` 이벤트 핸들러에서 매 프레임 `canvas.remove()` → `new fabric.Path()` → `canvas.add()`를 반복하는데, `remove`와 `add` 각각이 즉시 중간 렌더링을 트리거하여 "개체가 사라졌다 나타나는" 깜빡임이 발생했습니다. → 모든 remove/add 사이클을 `canvas.renderOnAddRemove = false`로 감싸서 중간 렌더링을 억제하고, 마지막에 `requestRenderAll()` 한 번으로 통합했습니다.
  3. `syncLinked()` 함수 내에서 `requestRenderAll()`을 매 프레임 중복 호출하여 렌더링 타이밍 충돌이 발생했습니다. → 제거하여 Fabric.js의 자체 렌더 사이클에 위임했습니다.
  4. 캔버스 경계 보정 시 부동소수점(floating-point) 오차가 누적되어 좌표가 매 프레임 미세하게 흔들렸습니다. → `Math.round(... * 100) / 100`으로 소수점 2자리 반올림하여 안정화했습니다.
  5. 개체 이동(`object:moving`) 핸들러에서 매 프레임 `getBoundingRect()` + `setCoords()` 이중 호출이 발생하여 렌더링이 답답한 속도 저하를 유발했습니다. → 경량화된 `left/top` + `width*scaleX` 기반의 경계 제한으로 교체하고 `setCoords()` 호출을 제거하여 성능을 크게 개선했습니다.
  6. 타원(Ellipse)은 `originX: 'center', originY: 'center'`로 생성되어 `left`/`top`이 중심점을 나타내는데, 경계 제한 코드가 이를 좌측 상단으로 간주하여 이동 가능 영역이 왼쪽 위로 치우쳐 있었습니다. → 모든 개체의 `originX`/`originY`를 반영한 오프셋 보정을 추가하여 정확한 경계 제한을 구현했습니다.
  7. `syncLinked()` 함수를 매 프레임 호출하는 대신, 연결된 텍스트/도형이 있는 경우에만 동기화 로직을 인라인으로 실행하도록 변경하여 함수 호출 오버헤드를 제거했습니다.
  8. `mouse:move` 이벤트 핸들러에서 도구가 비활성화된 상태(일반 개체 드래그 중)임에도 `canvas.getPointer()`, DOM 쿼리 등 모든 연산이 매 프레임 실행되고 있었습니다. → 도구 비활성화 + 그리기 중 아님 + 다중 클릭 그리기 중 아닌 경우 즉시 `return`하여 불필요한 연산을 완전히 차단했습니다.
  9. 개체 이동 시 간헐적인 끊김(프레임 드랍) 현상이 완전히 해결되었습니다. 기존에는 펜 선이나 도형이 완성된 이후에도 `objectCaching: false` 속성이 남아 있어, 드래그할 때마다 복잡한 벡터 연산이 매 프레임 재실행되며 성능을 크게 저하시켰습니다. 펜/도형/화살표가 확정되어 개별 개체로 변환될 때 기본값인 `objectCaching: true`를 따르도록 수정하여, 렌더링 시 캐시된 비트맵을 사용하도록 만들어 매우 부드러운 이동 성능을 확보했습니다.
  10. "저장" 버튼의 드롭다운 메뉴에 'SVG 저장' 옵션을 추가하고, 요청된 순서('PNG 저장', 'JPG 저장', 'SVG 저장', 'PDF 저장', 'JSON 저장')에 맞게 UI를 배치했습니다. (SVG 저장 로직 자체는 내부에 구현되어 있었으므로 버튼만 노출)
  11. 개체의 변(가장자리) 및 꼭짓점(조절점)의 마우스 인식 범위를 확대했습니다. 보이지 않는 클릭 허용 영역(`touchCornerSize`)을 기존 대비 2.4배로 넓히고, 여백(`padding`) 및 선 인식 오차(`targetFindTolerance`) 수치도 여유 있게 증가시켜, 마우스 커서를 정밀하게 맞추지 않아도 쉽게 개체를 선택하거나 크기를 조절할 수 있도록 개선했습니다.
  12. 우측 상단 모서리를 잡아 개체를 회전시킬 때 나타나는 회전 화살표(아이콘 및 마우스 커서)가 간혹 비정상적으로 크게 렌더링되거나 비율이 깨지는 현상을 해결했습니다. 커스텀 SVG 아이콘 데이터에 명시적인 크기(`width='16' height='16'`, `width='18' height='18'`) 속성을 추가하고, URI 문자열을 안전하게 인코딩(`encodeURIComponent`)하여 브라우저의 렌더링 오작동을 방지하고 항상 일관된 작은 흰색 아이콘으로 표시되도록 수정했습니다.
  13. 글상자가 적용된 도형(직사각형, 마름모, 타원) 회전 및 축소 시 텍스트 분리 및 위치 이동 오류 해결: 도형을 회전하거나 축소할 때 내부 텍스트가 도형의 움직임을 정확히 따라가지 못해 분리되던 현상을 수정했습니다. 특히 귀퉁이를 잡아 도형을 최소 크기 이하로 축소하려 할 때, 크기 제한으로 인해 반대편 모서리가 밀리며 도형 자체가 이동해버리는 버그를 해결하여, 제자리에서 안정적으로 최소 크기가 유지되도록 개선했습니다.
  14. 회전된 도형(직사각형, 마름모, 타원) 이동 시 경계선 충돌로 인한 오동작(텍스트 분리 및 화면 밖 이탈) 버그 수정: 회전된 도형을 드래그하여 이동시킬 때, 캔버스 화면 경계를 벗어나지 못하도록 막는 로직이 회전 각도를 전혀 고려하지 않아 도형이 비정상적으로 텔레포트하거나 텍스트와 분리되는 현상을 해결했습니다. 성능 저하를 일으키는 무거운 getBoundingRect() 호출 없이도 단순 삼각함수(sin, cos)만으로 회전된 바운딩 박스의 실제 면적을 빠르게 계산하도록 경계 제한 공식을 완전히 재설계하여, 압도적인 드래그 성능을 유지하면서도 회전된 도형이 화면 밖으로 빠져나가지 않고 내부 텍스트와 완벽하게 고정되도록 개선했습니다.
  15. 텍스트 정렬 아이콘 UI 추가: 일반 텍스트 및 도형 내 글상자 입력/수정 시 사용되는 토글창에 '왼쪽 맞춤', '가운데 맞춤', '오른쪽 맞춤' 아이콘 버튼을 명시적으로 추가했습니다. 크기, B, I, U, 정렬, 색상, 배경색 순으로 일관되게 배치하여 직관적으로 텍스트 정렬을 변경할 수 있도록 편의성을 크게 개선했습니다.
  16. 도형(글상자) 회전 시 내부 텍스트 동기화 버그 수정: 텍스트가 삽입된 도형을 회전시킬 때 텍스트가 즉각적으로 함께 회전하지 않고, 이후 이동을 해야만 위치가 맞춰지던 렌더링 누락 버그를 해결하여 회전 시에도 실시간으로 일체화되어 돌아가도록 수정했습니다.
  17. 도형(글상자) 세로 크기 조절 시 가로 너비 무한 확장 버그 수정: 도형의 상/하단 모서리(상변/하변)를 잡아 세로 길이만 조절할 때, 내부 텍스트의 가로 너비가 계속해서 비정상적으로 길어지던 버그를 수정했습니다. 텍스트의 실제 필요 최소 너비를 계산하여, 세로 크기 변경이 가로 크기에 영향을 주지 않도록 스케일링 로직을 개선했습니다.
  18. 도형 내부 텍스트 편집 시 도구 상태 유지 개선: 텍스트가 입력된 도형 내부를 클릭해 텍스트를 수정할 때, 상단바의 "텍스트" 도구 버튼이 강제로 활성화되어 기존 "도형" 작업 흐름이 끊기던 현상을 개선했습니다. 텍스트 편집 중에는 기존 도구(도형) 상태를 그대로 유지하되, 텍스트 속성 조절이 가능하도록 서브 토글창만 텍스트 패널로 자연스럽게 전환되며, 편집 종료 시 다시 기존 도형 패널로 자동 복귀하도록 수정했습니다.
  19. 일반 텍스트 폭 초기화 오류 해결: 처음 텍스트를 입력할 때 내부적으로 줄바꿈이 적용된 텍스트의 길이를 기준으로 너비를 재는 바람에, 가로 폭이 너무 좁게 고정되어 계속 강제 줄바꿈이 일어나던 현상을 수정했습니다. 이제 텍스트 상자 자체의 폭이 실제 입력된 한 줄짜리 글자의 너비를 정확히 따라가도록 개선되어, 엔터를 누르기 전에는 무제한으로 길게 입력할 수 있습니다.
  20. 객체 회전 시 기준점(Pivot) 동기화 오류 수정: 우측 상단(tr) 꼭짓점을 잡아 회전시킬 때 회전 중심축이 무조건 대상의 좌측 하단으로 쏠려 제멋대로 회전하는 버그를 해결했습니다. 이제 텍스트, 펜, 도형, 이모티콘, 이미지, 모자이크 등 **모든 개체가 항상 좌측 상단 꼭짓점을 축으로 고정하여 안정적으로 회전**하도록 통일했습니다.
  21. 전역 회전 마우스 커서 아이콘 변경: 텍스트, 도형, 이모티콘, 이미지, 모자이크 등 캔버스 내의 모든 개체에서 우측 상단 꼭짓점(회전 핸들)에 마우스를 올릴 때, 기존의 십자선(crosshair)이나 양방향 화살표 대신 직관적인 **'흰색의 작은 회전형 모양 화살표'** 마우스 커서가 나타나도록 커스텀 SVG 커서를 전역적으로 적용했습니다. (기타 크기 조절 모서리는 기존의 양방향 화살표 유지)
  22. 텍스트 편집 UX 전면 개선 (단일 클릭 편집 + I-beam 커서 + 블록 선택): 
      - **마우스 커서**: 텍스트 입력창(일반 텍스트 및 도형 내 글상자) 위에 마우스를 올리면 대문자 I 모양(I-beam)의 텍스트 커서가 나타나, 텍스트 편집이 가능함을 직관적으로 알 수 있습니다.
      - **첫 번째 클릭**: 텍스트 입력창을 한 번 클릭하면 해당 개체를 인식(외곽선 표시)하여 크기 조절 및 회전이 가능한 상태가 됩니다. 도형(직사각형, 마름모, 타원) 내부의 텍스트도 마찬가지로 도형 내부를 한 번 클릭하면 도형만 인식합니다.
      - **두 번째 클릭**: 이미 선택된 상태에서 텍스트 부분을 한 번 더 클릭하면, 클릭한 정확한 위치에 커서가 깜빡이며 즉시 편집 모드로 진입합니다. 이때 상단바의 "텍스트" 버튼은 활성화되지 않은 상태를 유지하며, 텍스트 토글(크기, B, I, U, 정렬, 색상, 배경색 등 설정창)만 하단에 나타납니다.
      - **도형 내 글상자 두 번째 클릭**: 도형(직사각형, 마름모, 타원)이 글상자로 된 경우, 텍스트 영역을 클릭해도 먼저 도형이 선택됩니다(`mouse:down`에서 `linkedShape`를 감지하여 도형으로 리다이렉트). 도형이 선택된 상태에서 텍스트 부분을 다시 한 번 클릭하면 즉시 텍스트 편집 모드로 진입합니다. (내부적으로 `__shapeJustSelected` 플래그로 첫 클릭/두 번째 클릭을 구분)
      - **블록 선택**: 편집 모드에서 마우스로 텍스트를 드래그하여 블록(범위)을 설정한 뒤, 텍스트 토글의 크기/B/I/U/정렬 등을 변경할 수 있습니다.
      - **편집 종료**: 텍스트 입력창 밖의 영역을 클릭하면 편집 모드가 종료되고 텍스트 토글이 사라집니다.
  23. 도형 모서리 조절 시 비율 축소/확대 오류 수정: 텍스트가 포함된 도형(직사각형, 마름모, 타원)의 꼭짓점(좌상, 좌하, 우하)을 잡고 크기를 축소할 때, 내부 텍스트의 최소 크기 제한에 걸리면 한쪽 축만 멈추고 다른 축은 계속 줄어들어 도형의 비율이 심각하게 깨지던(왜곡되던) 버그를 완전히 해결했습니다. 이제 최소 크기에 도달하더라도 가로/세로 비율을 유지하면서 안전하게 크기 제한이 적용됩니다. 또한 도형의 각 변(좌, 우, 상, 하)을 잡고 조절하는 기능도 방해 없이 독립적으로 동작하도록 스케일링 로직을 개선했습니다.
  24. 도형 내 글상자 편집 진입(두 번째 클릭) 불가 버그 수정: **진짜 근본 원인** — 첫 번째 클릭으로 도형이 선택되면 선택 핸들(조절점, 테두리)이 텍스트 위를 덮어버려, 두 번째 클릭 시 Fabric.js의 `findTarget`이 텍스트가 아닌 **도형을 타겟으로 반환**합니다. 즉 `o.target.linkedShape`(텍스트→도형) 조건을 통과하지 못하고 `o.target.linkedText`(도형→텍스트) 분기로 빠져, 매번 '첫 번째 클릭'으로만 인식되어 영원히 편집 모드에 진입하지 못했습니다. **수정**: `else if (o.target.linkedText)` 분기에도 두 번째 클릭 감지 로직(`_lastActiveShapeForLinkedText === shape`)을 추가하여, 도형이 타겟인 경우에도 `shape.linkedText`를 통해 텍스트 오브젝트를 찾아 `setTimeout(50ms)` 내에서 `canvas.setActiveObject(textTarget)` + `textTarget.enterEditing()`을 호출합니다. 또한 `setCursorByClick()` 호출을 제거하여 50ms 후 이벤트 객체 캔버스 컨텍스트 무효화로 인한 `getRetinaScaling` 에러도 함께 해결했습니다.
  25. 텍스트 편집 모드에서 드래그 블록 선택 불가 및 4방향 커서 버그 수정: **근본 원인** — Fabric.js 5.3.1의 IText는 `mouseUpHandler`에서 `__lastSelected` 플래그를 확인하여 두 번째 클릭 시 자동으로 `enterEditing`을 호출하는 **내장 편집 진입 메커니즘**을 갖고 있습니다. 또한 `_mouseDownHandler`에서 `this.selected`가 true이면 `setCursorByClick`을 호출하여 커서 위치를 설정하고, `initMouseMoveHandler`로 드래그 텍스트 선택을 처리합니다. 그런데 커스텀 `mouse:down` 핸들러에서 이 내장 메커니즘보다 **먼저** `enterEditing(o.e)`를 호출하면, 직후에 실행되는 IText의 `_mouseDownHandler`가 `setCursorByClick(t.e)`을 호출할 때 내부적으로 `this.canvas.getPointer(e)` → `this.canvas.getRetinaScaling()`에서 에러가 발생했습니다(`getRetinaScaling` TypeError). 이 에러가 IText 내부 상태 초기화를 중단시켜, 이후 마우스 드래그 시 `__isMousedown`이 정상 설정되지 않아 텍스트 선택(블록 지정)이 불가능했고, `_currentTransform`이 해제되지 않아 4방향 이동 커서가 계속 표시되었습니다. **수정**: (1) 일반 텍스트(standalone)의 경우 커스텀 `enterEditing` 호출을 **완전히 제거**하고 Fabric.js 내장 `mouseUpHandler`에 위임합니다. 이를 통해 IText의 `_mouseDownHandler` → `setCursorByClick` → `initMouseMoveHandler` 체인이 정상 실행되어 드래그 블록 선택과 I-beam 커서가 자연스럽게 동작합니다. (2) 도형 내 글상자(linkedShape/linkedText)는 Fabric.js 내장 메커니즘이 작동하지 않으므로(선택 핸들이 텍스트를 덮어 타겟이 바뀌기 때문) 커스텀 `enterEditing()`을 유지하되, 이벤트 객체 전달(`o.e`)을 제거하고 `canvas._currentTransform = null`로 트랜스폼을 해제합니다.
  26. 도형 텍스트 편집 직관성 개선, Word Wrap 및 도형 자동 조절 추가: 
      - 도형 내 글상자 클릭 시 `setCursorByClick`을 사용하여 마우스로 클릭한 정확한 위치에 즉시 커서가 배치되어 바로 수정할 수 있도록 개선했습니다.
      - 텍스트 편집 모드 진입/종료 시 도구 활성화 상태와 무관하게 텍스트 속성 토글창(크기, B, I, U, 정렬 등)이 자동으로 표시/숨김 처리되어 직관적인 편집 환경을 제공합니다.
      - 텍스트 속성 설정 변경 시 `updateActiveText`를 통해 선택된 도형에 귀속된 `linkedText`에도 실시간으로 스타일이 연동 적용되도록 수정했습니다.
      - 텍스트가 도형 너비에 도달하면 자동으로 줄바꿈(Word Wrap)이 적용되도록 `fabric.Textbox`를 적용했으며, 마름모와 타원은 내접 사각형 너비로 자동 계산하여 텍스트가 외곽선을 삐져나가지 않도록 완벽하게 처리했습니다.
      - 입력된 텍스트 양(줄 수)이 늘어나면 도형의 높이(scaleY)가 자동으로 늘어나며 텍스트를 감싸고, 텍스트를 지우면 생성 시점의 원래 크기로 자동 축소되도록 반응형 조절 로직을 도입했습니다.
  27. 도형 내 텍스트 편집 중 드래그 블록 선택 및 조작 불가 버그 수정:
      - 도형 내 글상자가 이미 편집 모드(커서 깜빡임)에 진입한 상태에서, 마우스 클릭으로 텍스트를 드래그하여 블록을 지정하거나 커서를 다른 위치로 이동시키려 할 때 다시 도형이 선택되며 편집이 해제되던 버그를 해결했습니다.
      - 텍스트가 편집 모드일 경우 커스텀 `mouse:down` 이벤트가 이를 감지하여 조기에 종료(return)하도록 함으로써, Fabric.js의 내장 텍스트 이벤트 처리 로직이 온전히 실행되어 자연스러운 블록 지정 및 편집이 가능해졌습니다.

  - 추가 이슈 해결 (2026-06): 도형의 가장자리를 잡아 크기를 조절할 때 시각적 크기가 따라오지 않던 현상(스케일 초기화 버그) 및 텍스트 툴바(B, I, U, 정렬, 크기)가 전역 변수 충돌로 인해 작동하지 않던 문제를 해결함.

  - 추가 이슈 해결 (2026-06): 글상자(도형 텍스트) 사용 시 마우스 커서 아이콘 로직 개선. 최초 텍스트 영역 위를 맴돌 때는 4방향 화살표(move)를 표시하고, 텍스트 편집 모드로 진입한(두 번째 클릭) 경우에만 대문자 I(text) 커서가 나타나도록 hoverCursor 상태를 동적으로 전환함.

  - 추가 이슈 해결 (2026-06): 도형 텍스트 내부를 두 번째 클릭하여 텍스트 편집 모드로 진입하는 로직이 도형의 외곽 크기 조절/회전 핸들(control)을 클릭할 때도 발동하여 조절 기능이 무시되던 현상 수정 (isControl 예외 처리). 텍스트 편집 모드에서 블록을 지정하여 특정 속성(굵기, 색상 등)을 변경할 때 전체 텍스트가 강제 동기화되던 문제를 해결하여 블록 지정 부분에만 해당 변경사항이 반영되도록 updateActiveText 구조 리팩토링.

  - 추가 이슈 해결 (2026-06): 
    - 도형 크기 조절(Scaling) 시 텍스트가 줄바꿈되면서 발생하는 높이 불일치 현상 및 비정상적인 크기 제한 버그 수정 (`object:scaling` 로직 최적화)
    - 마우스 드래그 이동/회전 시 텍스트가 실시간으로 일체화되지 않고 분리되던 시각적 지연 현상 수정 (`object:moving`에 `requestRenderAll` 추가)
    - 특정 상황에서 블록 지정 텍스트에 컬러 피커 변경이 적용되지 않던 문제 해결 (`targetColorBtn` 분기 세분화 및 도구 활성 상태 검사 제거)

    - 도형의 상변/하변 크기 조절 시 무효화되던 현상 수정 (`reqH` 상수 재할당 버그 및 `setPositionByOrigin` 미적용 문제 해결)
    - 텍스트 입력 및 글꼴 변경 시 높이가 도형 크기에 실시간 연동되도록 `changed` 이벤트 동기화 강화
    - 블록 지정 후 컬러 피커 사용 시 발생하던 `ReferenceError` 버그 픽스 및 정렬(Align) 속성이 부분 서식으로 오적용되던 문제 수정

    - 외곽선의 모서리 조절 시 가로세로 비율이 유지되도록 캔버스 설정(uniformScaling: true) 변경 및 상/하변 조절 시 도형 크기가 정상 변경되도록 제어점 동기화 순서(getPointByOrigin 선 호출) 수정.


### 2026-06-11 UI/UX 아이콘 컬러 코딩 및 핀 시인성 강화
- **개요:** 벡터 방식(Lucide)을 유지하면서 각 아이콘의 성격별로 컬러를 부여하여 시인성을 높이고, 핀(고정) 기능의 UI 피드백을 강화했습니다.
- **구현 내용:**
  - 캡처 도구(블루), 편집 도구(퍼플), 추가/삽입(오렌지), 변형/수정(그린), 시스템 및 옵션(슬레이트/그레이), 지우개 및 닫기 등 파괴적 행동(레드)으로 테마 색상을 지정해 적용 완료했습니다.
  - index.html과 guide.html 양쪽에 동기화된 색상이 CSS(stroke)로 자연스럽게 스며들도록 반영했습니다.
  - 상단바의 핀 6개에 대해 모두 붉은색(stroke: #fca5a5)을 적용하고, 마우스 호버 시 쨍한 레드, 활성화 시 레드 드롭 섀도우 효과(ilter: drop-shadow)를 추가했습니다. (핀의 absolute 포지셔닝 레이아웃 복원 포함)
  - guide.html 내부 아이콘들이 정상적으로 렌더링되지 않던 문제를 수정하여, 가이드라인 문서를 열었을 때 상단바와 동일한 형태와 색상의 벡터 아이콘이 정확히 로드되도록 개선했습니다.

### 2026-06-11 UI/UX 고해상도 벡터 아이콘 전면 개편 및 안정화
- **개요:** 기존에 혼용되어 사용되던 텍스트 이모지(Emoji) 및 개별 SVG 아이콘들을 세련되고 통일된 고해상도 벡터 방식인 **Lucide Icons** 오픈소스 라이브러리로 완전히 교체했습니다.
- **상세 내역:**
  - 상단 툴바의 모든 도구 버튼(새캡처, 뒤로, 텍스트, 펜, 도형, 지우개 등) 및 파일 작업 버튼을 선형(Line-art) 벡터 아이콘으로 일괄 변경하여 시각적 통일성과 전문성을 크게 향상시켰습니다.
  - 서브 패널(텍스트 정렬, 휴지통, 핀 고정) 및 모달 팝업의 타이틀 등에도 모두 일관된 Lucide SVG 아이콘(data-lucide)을 적용했습니다.
  - 인터넷망이 연결된 환경에서 항상 최신 고품질 벡터 그래픽으로 표시되도록 CDN 방식을 적용하였으며, CSS(tn-tool i 등)를 미세 조정하여 아이콘 정렬과 레이아웃이 기존과 이질감 없이 완벽하게 유지되도록 최적화했습니다.
  - 툴바 6개 버튼(텍스트~모자이크)의 핀 고정 기능이 SVG 변환 과정에서 클릭 이벤트를 잃어버리던 버그를 수정하여 정상 작동하도록 복구했습니다.
  - 프로그램 사용법 가이드(guide.html) 내부의 아이콘들도 상단바와 동일한 벡터 아이콘으로 모두 일치시켰습니다.

### 2026-06-07 도형 세로 길이 조절 및 텍스트 블록 설정 개선

#### 도형 세로 크기 조절 근본 수정
- **`uniformScaling: false`로 변경:** `uniformScaling: true`는 모든 핸들(상/하/좌/우 포함)이 비율 유지 모드로 동작하여 세로만 독립 조절이 불가능했음. `false`로 변경하여 상/하변 핸들은 세로만, 좌/우변 핸들은 가로만 독립적으로 조절 가능하도록 수정.

- **`object:scaling` 이벤트에서 `originalWidth/Height` 매 프레임 덮어쓰기 제거:** 기존에는 드래그 중 매 프레임마다 `originalWidth/Height`를 현재 값으로 갱신하여, 최소 크기 제한(clamp)의 기준점이 유실되는 버그 존재. 초기 생성 시점의 값만 보존하도록 수정.
- **`object:modified`에서 linkedText가 있는 도형의 `normalizeScale` 건너뛰기:** `normalizeScale`은 오브젝트를 재생성하여 `linkedText` 바인딩이 끊어지는 심각한 문제 존재. 대신 `rect`는 `width/height`에, `ellipse`는 `rx/ry`에, `polygon`은 `points` 좌표에 scale을 직접 반영(bake)하고 scale을 1로 리셋하는 방식 적용.
- **`syncShapeToText` 함수 단순화:** 복잡한 수학적 오프셋 계산(dx/dy/rad) 대신, 도형 타입별 텍스트 영역 비율(`textAreaFactor`, `heightFactor`)을 사용하여 최소 가로/세로 크기 제한만 적용하는 깔끔한 구조로 재작성.

#### 텍스트 자동 확대/축소 및 줄바꿈
- **`textObj.on('changed')` 핸들러 개선:** `originalHeight * scaleY` 대신 현재 `height * scaleY`를 사용하여, `normalizeScale` 후(scale=1, height=실제크기) 정확한 높이 비교가 되도록 수정.
- **`_initHeight` 속성 추가:** 글상자 생성 시점의 시각적 높이를 저장하여, 텍스트를 지울 때 이 높이까지만 자동 축소되도록 복원 로직 적용.
- **마름모/타원 내부 텍스트 영역:** `cos(π/4)` (약 0.707) 비율로 텍스트 영역 너비/높이를 계산하여 도형 경계 안에 텍스트가 머무르도록 보장.

#### 텍스트 블록 선택 서식 적용 수정
- 색상 팔레트(.palette-cell) 및 컬러 버튼에 `mousedown` 이벤트 시 `e.preventDefault()`를 추가하여 텍스트 편집 중 포커스가 유실되지 않도록 수정.
- `updateActiveText` 함수에서 `targetText.dirty = true; targetText.initDimensions();` 호출을 추가하여 선택 영역 서식이 즉시 반영되도록 개선.

#### 모든 개체의 세로 길이 조절 근본 수정 (2026-06-07 추가)
- **근본 원인:** `cropX`/`cropY` 함수가 `mt`(상변), `mb`(하변), `ml`(좌변), `mr`(우변) 핸들의 `actionHandler`를 덮어쓰고 있었음. 이 함수 내부에서 `!target.isMediaImage && target.type !== 'image'` 조건으로 분기하는데, 이모티콘이 `fabric.Image`로 생성되어 `type === 'image'`이므로 crop(자르기) 로직으로 진입하여 스케일링 대신 자르기만 실행됨.
- **수정:** 조건을 `!target.isMediaImage`로 변경하여, **오직 `isMediaImage: true`인 미디어 이미지만** crop 동작을 수행하고, 이모티콘(`isEmoji`)·도형·텍스트 등 나머지 모든 개체는 표준 `scalingX`/`scalingY`로 세로/가로 독립 조절이 가능하도록 수정.

#### Textbox 컨트롤 얕은 복사(shallow clone) 문제 수정 (2026-06-07 추가)
- **근본 원인:** `fabric.util.object.clone(controls)`는 1단계만 복사하는 얕은 복사(shallow clone)임. 따라서 `fabric.Textbox.prototype.controls.tl`은 원본 `controls.tl`과 **동일한 객체 참조**를 공유함. Textbox 전용으로 `tl.actionHandler = changeWidth`를 설정하면, 원본 `controls.tl.actionHandler`까지 함께 변경되어 **모든 개체(도형, 이모티콘, 이미지 등)의 꼭짓점 핸들러가 `changeWidth`(가로만 변경)**로 바뀌는 심각한 버그 발생.
- **수정:** `Object.keys(controls).forEach(key => { textboxControls[key] = new fabric.Control(Object.assign({}, controls[key])); })` 방식으로 각 컨트롤 객체를 개별적으로 깊은 복사하여 Textbox 전용 controls 세트를 완전히 분리. 이로써 Textbox의 핸들러 변경이 다른 개체 타입의 핸들러에 영향을 미치지 않음.

#### 모자이크 블록 크기 기본값 변경 (2026-06-07 추가)
- **변경 내용:** 모자이크 블록 크기 기본값을 5에서 3으로 수정하여 상단바와 환경설정의 기본값을 일치시킴.

#### 도형 글상자 크기 조절 시 최소 크기 도달에 따른 위치 이동(밀림) 버그 수정 (2026-06-08 추가)
- **근본 원인:** 텍스트가 삽입된 도형(직사각형, 마름모, 타원)의 하변이나 우측 하단 등 모서리를 잡고 크기를 축소할 때, 내부 텍스트 크기에 의한 최소 크기 제한(clamp)에 도달하면 `scaleX`/`scaleY`만 고정되고 `top`/`left` 중심 좌표는 마우스 이동을 따라 계속 계산되어 도형 전체가 밀려버리는(이동하는) 현상이 발생함.
- **수정:** `syncShapeToText` 함수 내에 스케일 제한이 적용되기 전의 고정 앵커 포인트(`translateToOriginPoint`)를 기억해두는 로직을 추가함. 제한(clamp)이 발생한 경우 `setPositionByOrigin`을 호출하여 조절 중인 핸들의 반대편 기준점 위치를 원래 위치에 강제로 고정시켜, 도형의 축소가 멈추면 위치 이동도 완벽하게 멈추도록 수정함.

#### 마름모(Polygon) 글상자의 외곽선(Bounding Box) 오작동 버그 수정 (2026-06-08 추가)
- **근본 원인:** 도형에 텍스트가 추가된 상태(글상자)에서 크기를 조절한 뒤 마우스를 떼면(`object:modified` 이벤트), 선 굵기 왜곡을 방지하기 위해 내부적으로 배율(`scaleX`, `scaleY`)을 각각 `width`/`height`(직사각형, 타원) 또는 `points`(다각형) 좌표에 직접 구워넣는(bake) 로직이 실행되었음. 직사각형과 타원과 달리, 다각형(마름모)의 경우 `points`를 조작한 직후 Fabric.js의 내부 `_calcDimensions()`가 호출되면서 앵커 포인트 오프셋(`pathOffset`)이 재계산되어 실제 시각적 외곽선 위치와 어긋나는 현상이 발생함.
- **수정:** `index.html`에서 전역적으로 `fabric.Object.prototype.strokeUniform = true`가 이미 적용되어 있으므로 배율을 초기화하지 않아도 선 굵기가 왜곡되지 않음이 확인됨. 따라서 `object:modified`의 강제 배율 초기화(bake) 예외 처리 로직에서 `polygon`을 제거함. 글상자가 된 마름모도 일반 마름모와 동일하게 `scaleX`, `scaleY` 속성을 유지하도록 변경하여, 시각적 외곽선이 마름모 외부에 항상 정확하게 일치하도록 버그를 해결함.

#### 편집 과정에서 "뒤로(Undo) / 앞으로(Redo)" 실행 시 도형과 텍스트가 분리되는 현상 완벽 수정 (2026-06-08 추가)
- **근본 원인:** "뒤로(Undo)"나 "앞으로(Redo)" 버튼을 누르면 내부적으로 `canvas.loadFromJSON()` 함수가 호출되며 캔버스 위의 모든 객체들이 완전히 새로운 객체로 재생성됨. 이때, 기존에 런타임 변수로 연결해 두었던 도형과 텍스트 간의 메모리 참조 연결 고리(`linkedText`, `linkedShape`)와 이벤트 리스너가 모두 증발해버리면서, 다시 불러온 객체들은 텍스트 크기 조정 연동이 끊어지고 도형 이동 시 텍스트가 따라오지 않는 '완전 분리' 상태가 됨.
- **수정:** 도형을 글상자로 변환할 때, 도형과 텍스트 각각에 고유 ID(`id`, `linkedTextId`, `linkedShapeId`)를 부여하고 `fabric.Object.prototype.toObject`를 통해 JSON 저장 과정에 이 커스텀 속성들이 포함되도록 직렬화(Serialize)함. 그리고 `canvas.loadFromJSON()`이 실행될 때마다 `restoreLinkedTexts()` 함수가 자동 실행되어, 각 객체의 고유 ID를 대조하여 분리된 도형과 텍스트를 다시 하나로 단단하게 묶고(Link) 텍스트 높이 변화에 맞춘 도형 자동 조절 이벤트 리스너를 다시 바인딩하도록 수정하여 분리 문제를 원천적으로 차단함. Z-index(레이어 순서) 또한 텍스트가 항상 도형 위로 올라오도록 겹침 보정 로직을 포함함.

#### 글상자의 '내용 입력' 문구 Placeholder(플로팅 텍스트) 기능 완벽 구현 (2026-06-08 수정)
- **개요:** 기존에 '내용 입력'이라는 실제 문자열을 삽입해 두었다가 클릭 시 지우는 방식에서 벗어나, 텍스트 객체의 실제 내용(`text`)은 빈 문자열(`""`)로 유지하되 화면에만 임시로 '내용 입력'이 그려지는 진정한 플로팅 텍스트(Floating Text) 방식으로 재구현함.
- **기능 상세:**
  - 글상자를 최초 생성하면 `text` 속성은 비어 있으며, 내부 `_renderText` 렌더링 오버라이드를 통해 회색(`#999999`)의 '내용 입력' 가상 문구가 캔버스에 직접 그려짐. (실제 텍스트가 아니므로 글자를 드래그하여 선택할 수 없음)
  - 사용자가 편집을 위해 글상자를 클릭(포커스/`selected` 이벤트)하는 즉시 가상 문구가 사라지며 비어 있는 입력창 상태가 됨.
  - 아무 내용도 입력하지 않고 바깥을 클릭하여 선택을 해제(`deselected` 이벤트)하면 다시 '내용 입력' 가상 문구가 복원됨.
  - 텍스트 색상(`fill`)을 임의로 변경하거나 조작하는 불필요한 로직이 제거되어, 사용자가 선택한 색상이 실제 타이핑 시점에 완벽하게 적용되며 버그 발생 가능성이 원천 차단됨.

#### 객체 회전 시 플로팅 각도 표시 기능 개선 (2026-06-08 추가)
- **개요:** "텍스트, 펜, 도형, 이모티콘, 이미지, 모자이크" 등 캔버스에 삽입된 모든 개체를 마우스로 회전시킬 때 마우스 포인터 근처에 회전 각도를 플로팅 툴팁 형태로 실시간 표시함.
- **기능 상세:**
  - `scaleAndRotate` 커스텀 핸들러뿐만 아니라 기본 `object:rotating` 이벤트에도 동일한 툴팁 로직(`floating_angle_tooltip`)을 결합하여, 개체의 종류와 무관하게 모든 회전 동작 시 각도가 표시됨.
  - 각도는 소수점 1자리까지 표시되도록 포맷팅(`toFixed(1)`)을 적용함 (예: `45.0°`, `12.5°`).
  - 마우스 클릭 해제 시(`window.mouseup`) 툴팁이 자동으로 숨겨짐.

#### 개체 종류별 꼭짓점 드래그 비율 유지(Aspect Ratio) 기능 세분화 (2026-06-08 추가)
- **개요:** 모서리 컨트롤(꼭짓점)을 잡아 드래그할 때, 개체의 속성(타입)에 따라 가로/세로 비율 유지 여부를 다르게 적용함.
- **기능 상세:**
  - **이모티콘, 이미지, 모자이크:** 원본 이미지가 왜곡되지 않도록 꼭짓점을 잡아 드래그 시 **항상 가로/세로 비율이 유지**되면서 확대/축소됨 (`scalingEqually`).
  - **텍스트, 펜, 도형:** 꼭짓점을 잡고 자유롭게 비례와 무관하게(스트레치) 크기를 조절할 수 있도록 자유 변형을 허용함 (`scalingXOrY`). (필요 시 `Shift` 키를 누르면 비율 유지가 가능함)

#### 글상자(도형) 및 이미지류 레이어 순서(맨 뒤로, 맨 앞으로) 동기화 및 버튼 적용 버그 수정 (2026-06-09 추가)
- **개요:** 이모티콘, 이미지, 모자이크 개체에서도 '좌우반전', '상하반전', '맨 뒤로', '맨 앞으로' 버튼이 정상적으로 작동하도록 수정하였으며, 해당 개체들에는 '글상자로' 버튼이 표시되지 않게 변경함. 또한 직사각형, 마름모, 타원이 글상자로 된 경우 도형이나 텍스트의 레이어 순서를 변경할 때 텍스트와 도형이 함께 이동하도록 동기화함.
- **기능 상세:**
  - 이모티콘, 이미지, 모자이크 개체를 선택했을 때 '글상자로' 기능은 불필요하므로 제외하고 반전 및 레이어 버튼만 나타나도록 `hasControls`와 `controls` 설정 로직 보완.
  - 도형 글상자에서 '맨 뒤로' 또는 '맨 앞으로' 버튼을 클릭하거나 선택 시 자동으로 활성화되는 레이어 순서 변경(Auto BringToFront) 시, `linkedText`와 `linkedShape`를 확인하여 도형과 내부 텍스트의 레이어가 어긋나지 않고 함께 최상단 혹은 최하단으로 이동하도록 수정함. 텍스트가 항상 도형보다 위에 렌더링되도록 `bringToFront` 및 `sendToBack` 호출 순서를 엄격히 제어함.

#### 환경설정 텍스트 UI 레이아웃 개선 및 정렬 옵션 추가 (2026-06-09 추가)
- **개요:** 환경설정(`modal_settings`) 내 "텍스트 설정" 섹션의 UI 배치를 요청에 따라 간결하고 직관적인 한 줄 배치 형태로 개선하였으며, 기존에 누락되었던 텍스트 정렬 옵션을 환경설정 창 내에 추가하고 메인 툴바와 동기화되도록 연동함.
- **기능 상세:**
  - 1번 줄: "텍스트 크기", "B(굵게)", "I(기울임)", "U(밑줄)" 설정 항목을 한 줄에 나란히 배치함.
  - 2번 줄: "텍스트 정렬" 항목을 새로 추가하고, '왼쪽 맞춤', '가운데 맞춤'(기본값), '오른쪽 맞춤' 아이콘 버튼을 한 줄로 배치함. 해당 버튼 변경 시 상단 메인 툴바의 텍스트 정렬 버튼(`text_align`)과 즉시 동기화되도록 `applySettings` 내부 저장 및 불러오기 로직에 추가함.
  - 3번 줄: "글자 색상", "입력창 배경색", "입력창 투명도" 설정 항목을 한 줄에 나란히 배치하여 사용자의 가독성과 조작 편의성을 높임.

#### 텍스트 토글 패널 내 "투명도" 기능 추가 (2026-06-10 추가)
- **목적:** 텍스트 토글 패널(`.sub-panel#panel_text`)에 "투명도" 입력창을 추가하여, 전역 "환경설정"의 "입력창 투명도"와 동일하게 작동하도록 동기화함.
- **수정 사항:**
  - `index.html` 내 텍스트 패널 요소에 `<input type="number" id="text_bg_opacity_input">` 요소를 새로 추가.
  - 해당 입력창과 환경설정의 `set_txt_bg_opacity` 요소가 서로 연동되도록 `addEventListener('input')` 및 `addEventListener('change')` 이벤트 추가.
  - 앱 로드 및 `applySettings()` 호출 시 양쪽의 투명도 값이 동일하게 유지되도록 동기화 로직 보강.

#### 텍스트 개체 더블클릭 시 속성 수정 창 비활성화 (2026-06-10 추가)
- **목적:** 텍스트 개체는 자체에서 직접 편집이 가능하므로, 불필요하게 '개체 속성 수정' 팝업 창이 나타나지 않도록 개선함.
- **수정 사항:**
  - `index.html`의 캔버스 더블클릭 이벤트 핸들러(`canvas.on('mouse:dblclick')`) 내에 `if (isText && !isEmoji) return;` 조건을 추가.
  - 이모지 개체가 아닌 일반 텍스트 개체를 더블클릭할 경우 즉시 핸들러를 종료하여 속성 창이 나타나지 않게 함.
#### 텍스트 속성 조절 편의성 및 버그 수정 (2026-06-10 추가)
- **개요:** 텍스트 입력 관련 편의성을 강화하고, 비활성화된 상태에서 의도치 않게 연속으로 텍스트가 입력되는 오류를 해결함.
- **기능 상세:**
  - **텍스트 배경 투명도 조절 기능 추가:** 기존 환경설정 탭에서만 조절 가능했던 '텍스트 배경 투명도(Opacity)' 조절 슬라이더를 텍스트 편집 상단 속성 패널(토글창)에 직접 추가하여, 텍스트 편집 중에 즉각적으로 투명도를 조절할 수 있도록 개선함.
  - **더블 클릭 속성창 제거:** 캔버스에 삽입된 텍스트 개체를 더블 클릭할 때 불필요한 '개체 속성 수정' 팝업창이 나타나지 않도록 차단함. 텍스트는 개체 내부 자체 클릭으로 모든 텍스트 편집이 가능하므로, 오동작을 방지하고 자연스러운 타이핑 UX를 유도함.
  - **텍스트 연속 입력(버튼 비활성화 무시) 버그 완벽 수정:** 
    - **근본 원인:** 텍스트 입력 후 캔버스 바깥 영역이나 다른 버튼을 클릭하여 텍스트 편집을 종료할 경우, 텍스트 버튼의 시각적 활성화 상태(.active 클래스)만 해제되고 내부 논리적 상태(activeTool 변수)는 여전히 'text'로 남아있어, 이후 캔버스를 무심코 클릭할 때마다 새로운 텍스트 개체가 원치 않게 생성되는 버그가 있었음.
    - **수정 사항:** 텍스트 편집이 공식적으로 종료되는 시점(	ext:editing:exited 이벤트)에 핀(고정) 모드가 꺼져 있다면 즉각적으로 ctiveTool 논리 상태까지 강제로 
ull로 초기화하도록 로직을 재설계함. 이를 통해 핀 고정 상태가 아닐 때 텍스트 작업을 마치면 어떠한 상황에서도 텍스트 도구가 완벽히 해제되도록 수정하여 연속 생성 오류를 차단함.
#### 펜 및 도형 속성창 UI/UX 개선 및 환경설정 최적화 (2026-06-10 추가)
- **개요:** 펜 도구와 도형 도구의 편의성을 높이기 위해 상단 토글 속성창과 환경설정 레이아웃을 개편함.
- **기능 상세:**
  - **펜 토글창 속성 추가:** 펜을 선택했을 때 나타나는 상단 토글창에 '배경 색상'과 '배경 투명도'를 추가하여 펜의 배경을 즉시 조절할 수 있도록 개선함.
  - **형광펜 전용 속성 분리:** 펜 토글창에 '형광펜 농도' 설정을 추가하되, 펜 종류가 '볼펜'일 때는 숨기고 '형광펜'을 선택했을 때만 나타나도록 동적으로 UI가 변경되게 구현함.
  - **도형 토글창 속성 추가:** 도형 토글창에 기존의 선/배경 색상에 더해 '배경 투명도' 입력 필드를 추가함.
  - **환경설정(Settings) 레이아웃 1줄 배치(Inline):** 
    - **펜 설정:** 볼펜 선 굵기/색상, 형광펜 선 굵기/색상/농도, 펜 배경 색상/투명도 등 연관된 속성들을 각각 한 줄(1-Row)로 깔끔하게 배치하여 가독성을 높임.
    - **도형 설정:** 도형의 선 굵기/색상, 배경 색상/투명도를 각각 한 줄로 묶어 깔끔한 UI를 제공함.
#### 개체 속성 수정 UI 보완 (2026-06-10 추가)
- **개요:** 기존 개체들의 속성 수정 모달(창)에서 부족했던 UI를 추가하고 텍스트를 개선함.
- **기능 상세:**
  - **펜 및 도형 개체:** 속성 수정 창의 '배경색' 오른쪽에 '배경 투명도' 속성을 추가함. '비율유지' 텍스트를 띄어쓰기가 적용된 '비율 유지'로 변경함.
  - **이미지 개체:** '가로세로 비율 유지' 텍스트를 간결하게 '비율 유지'로 변경함.
  - **이모티콘 및 모자이크 개체:** 크기 조절 시 단순 '크기' 입력란 대신, 이미지 개체와 동일하게 '가로x세로' 크기 입력란 및 '비율 유지' 체크박스를 제공하여 세밀한 비율 조절이 가능하도록 개선함.
#### 개체 속성 수정 UI 버그 수정 및 통합 (2026-06-10 추가)
- **개요:** 이미지, 모자이크 개체의 속성 수정 창에서 '크기(숫자 하나)'와 '크기(가로x세로)'가 중복으로 표시되던 UI 버그를 해결하고 이모티콘 개체 또한 이미지와 동일한 비율 제어 UI를 사용하도록 완전히 통합함.
- **상세 내용:**
  - 기존에 이모티콘 전용으로 존재하던 orm_edit_emoji (단일 크기 입력란)가 모달 창이 닫힐 때 초기화되지 않아, 이후 이미지나 모자이크 개체를 수정할 때 단일 크기 입력란이 중복으로 노출되는 버그(이전 상태 잔존)가 있었음.
  - 이모티콘 전용 수정 폼(orm_edit_emoji) HTML 및 관련 JavaScript 제어 로직을 코드에서 완전히 제거함.
  - 이모티콘, 이미지, 모자이크 개체가 모두 일관되게 orm_edit_image 폼을 공유하도록 로직을 재구성하여, 세 개체 모두 단일 '크기' 입력란 없이 '가로x세로' 입력란 및 '비율 유지' 옵션만 깔끔하게 표시되도록 수정함.
#### 자르기(Crop) 렌더링 동기화 개선 (2026-06-10 추가)
- **개요:** "자르기" 도구로 이미지를 자른 직후 현재 캔버스에 잘린 상태가 즉각적으로 시각적으로 반영되지 않고 왜곡되어 보이거나 갱신되지 않던 문제를 해결함.
- **상세 내용:** performEdgeCrop 함수에서 캔버스 내부 크기(Width, Height)를 변경한 직후 DOM 래퍼 크기를 강제로 동기화하는 syncFabricContainerSize() 함수를 호출하도록 수정함. 이를 통해 자르기 실행 즉시 브라우저 렌더링에 변경된 캔버스 크기가 완벽하게 적용되며 이후 pplyFitZoom()이 정확히 계산됨.

#### UI/UX 색상 미세 조정 (2026-06-11)
- **자르기(Crop) 아이콘 색상 변경**: 상단바 및 가이드 문서의 자르기 도구 아이콘 색상을 기존 에메랄드(초록, #10b981)에서 캡처/기본 기능 그룹과 동일한 블루(#3b82f6)로 변경하여 일관성을 맞추었습니다.
- **모자이크(Mosaic) 아이콘 색상 복구**: 자르기 도구 색상 변경 시 의도치 않게 같이 파란색으로 묶였던 모자이크 도구 아이콘 색상을 원래 기획 의도에 맞게 다시 에메랄드(초록, #10b981) 색상으로 분리 및 복구했습니다.

#### 캔버스 개체 고품질 벡터화 및 이모티콘 외곽선 정밀 매핑 (2026-06-11 추가)
- **개요:** 고해상도(Retina) 디스플레이에서 캔버스 개체가 흐릿하게 보이거나 확대 시 픽셀이 깨지는 현상을 방지하기 위해 렌더링 엔진을 벡터 기반으로 전면 개편했습니다.
- **상세 내용:**
  - **Retina 스케일링 적용:** 캔버스 초기화 시 `enableRetinaScaling: true`를 적용하여 실제 픽셀 밀도(DPI)에 맞게 1:1 매핑을 수행, 모든 선과 텍스트의 해상도를 극한으로 끌어올렸습니다.
  - **글로벌 캐싱 해제:** `fabric.Object.prototype.objectCaching = false`를 전역 설정하여 개체를 확대/축소할 때 캐시된 임시 이미지가 아닌 실시간 벡터 렌더링을 하도록 강제해 깨짐 현상을 완전히 제거했습니다.
  - **이모티콘 벡터화 및 정밀 Bounding Box:** 이모티콘을 캔버스 구석에서 몰래 PNG 이미지로 렌더링 후 삽입하던 비효율적인 로직을 버리고, 이모티콘의 정확한 픽셀 경계(actualBoundingBox)를 측정한 뒤 이를 즉석에서 SVG(벡터 이미지) 문자열로 래핑하여 `fabric.Image`로 삽입하도록 로직을 재설계했습니다. 이를 통해 ① 극한으로 확대해도 깨지지 않는 **완벽한 벡터 화질**과 ② 이모티콘 주변에 불필요한 여백 없이 쫀득하게 달라붙는 **정밀한 선택 외곽선(Bounding Box)** 두 마리 토끼를 모두 잡았습니다.

#### 이모티콘 팝업(피커) "최근 선택" UI 구현 및 개선 (2026-06-11 추가)
- **개요:** 이모티콘 피커 하단에 고정되어 있던 무의미한 기본 이모지 행(Skin-tone Picker 영역)을 제거하고, 사용자가 실제로 최근에 삽입한 이모티콘들이 순차적으로 저장되고 나열되도록 UI를 개편했습니다.
- **상세 내용:**
  - **불필요한 하단 행 제거:** `emoji-picker::part(skintone-picker)`에 CSS `display: none;`을 적용하여 사용성이 떨어지는 고정된 하단 행을 깔끔하게 숨겼습니다.
  - **최근 선택 목록 바(Bar) 추가:** 이모티콘 피커 바로 아래에 '최근 선택' 영역(`#recent_emojis_container`)을 신규로 배치했습니다.
  - **슬라이드(스크롤) 기능 지원:** 최근에 선택한 이모티콘 개수가 늘어나도 UI가 망가지지 않도록 가로 스크롤(슬라이드)을 지원하며, 쾌적하게 넘겨볼 수 있습니다.
  - **로컬 스토리지 연동:** 선택한 이모티콘 내역은 브라우저의 `localStorage`(`capcraft_recent_emojis`)에 저장되어, 앱을 껐다 켜도 자주 쓰는 이모티콘이 그대로 유지됩니다. (최대 30개 보관)

#### 레이어 조작 버튼 변경 및 자동 최상단 배치 로직 보완 (2026-06-11 추가)
- **개요:** 여러 개체가 겹쳐 있을 때 세밀하게 층위를 조절할 수 있도록 개체 조작 버튼 기능을 한 단계 단위로 변경하고, 마우스로 새로 선택한 개체가 묻히지 않고 무조건 맨 위로 올라오도록 로직을 보강했습니다.
- **상세 내용:**
  - **한 단계 뒤로/앞으로 변경:** 기존 '맨 뒤로(sendToBack)'와 '맨 앞으로(bringToFront)' 버튼이 내부적으로 각각 sendBackwards와 ringForward를 호출하도록 수정했습니다. 이때 텍스트가 결합된 도형 등 연결된 개체(Linked objects)가 서로의 순서를 보존하며 안정적으로 한 층씩 이동할 수 있도록 세밀한 예외 처리를 반영했습니다.
  - **자동 최상단 배치 보강:** mouse:down 시 캔버스의 활성 개체 상태를 확인하여, **'새로 마우스로 클릭하여 선택하는 순간'**에만 자동으로 개체가 맨 위로(ringToFront) 튀어오르도록 하여 직관적인 조작감을 유지합니다. 한편 이미 선택되어 활성화된 개체를 다시 드래그하거나 컨트롤할 때는 이 로직이 무시되게끔 설계(getActiveObject() !== o.target)하여, 한 단계 뒤로 보낸 객체도 그 위치를 유지한 채 자유롭게 이동시킬 수 있도록 했습니다.
  - **안전성 확보:** 배경 캡처 이미지는 캔버스 개체가 아닌 배경 이미지(backgroundImage) 속성으로 분리되어 있어, 아무리 뒤로 보내더라도 캡처 이미지 뒤로 숨는 일이 구조적으로 발생하지 않습니다.

#### 지우개 마우스 커서 아이콘 일원화 (2026-06-11 추가)
- **개요:** 지우개 도구 사용 시 화면에 표시되는 사용자 정의 마우스 커서를 기존의 3D 직육면체 모양에서 상단바 메뉴의 지우개 아이콘(Lucide 빨간색 지우개)과 동일한 디자인으로 변경했습니다.
- **상세 내용:** CSS의 #custom_eraser_cursor background-image 속성을 기존의 복잡한 커스텀 SVG에서 Lucide 라이브러리의 eraser SVG(stroke='%23ef4444') 문자열로 완전히 교체하여, UI 디자인의 일관성을 높이고 가시성을 개선했습니다. 지우개 굵기에 따라 커서 크기가 연동되는 기존 동작은 그대로 유지됩니다.

#### 도구 고정(Pin) 아이콘 활성화 시각적 개선 (2026-06-11 추가)
- **개요:** 상단바의 텍스트, 펜, 도형, 이모티콘 등의 기능 버튼 우측 상단에 위치한 '핀(Pin)' 버튼의 활성화 상태가 시각적으로 잘 띄지 않던 문제를 개선했습니다.
- **상세 내용:** 핀이 비활성화된 상태일 때는 기존처럼 속이 빈 테두리 아이콘 상태를 유지하지만, 핀이 활성화(ctive)되었을 때는 아이콘 내부가 빨간색으로 꽉 채워지도록 CSS .pin-icon.active 속성에 ill: #ef4444; 를 추가했습니다. 이를 통해 기능의 고정 여부를 직관적으로 한눈에 파악할 수 있게 되었습니다.

#### 저장 다이얼로그 포맷 지정 자동화 및 SVG 저장 지원 (2026-06-11 추가)
- **개요:** 저장 시 OS 기본 다이얼로그의 '파일 형식(T)'이 기본적으로 'All Files' 등으로 지정되던 현상을 해결하여, 사용자가 선택한 저장 형식(png, jpg, svg, pdf, json)이 다이얼로그의 기본 파일 형식 필터로 자동 지정되도록 개선했습니다.
- **상세 내용:** 브라우저 기본 다운로드 동작 대신 Python 백엔드의 네이티브 파일 저장 다이얼로그(QFileDialog.getSaveFileName)를 호출하도록 프론트엔드-백엔드 브릿지를 복원했습니다. 사용자가 요청한 포맷에 따라 initial_filter가 정확히 매핑되며, 다이얼로그 내에서 사용자가 다른 포맷으로 재지정할 경우 해당 포맷으로 데이터가 동적으로 재생성되어 저장되도록 execute_save_to_path 콜백 로직을 구현했습니다. 또한 SVG 저장이 PNG로 처리되던 문제를 수정하여 정상적인 SVG 파일 저장을 지원합니다.

#### 개체 조작 편의성 및 마우스 인식 범위 개선 (2026-06-11 추가)
- **개요:** 캔버스 위의 개체를 선택하거나 크기 조절/회전 등을 수행할 때, 마우스 포인터가 정확히 외곽선이나 꼭짓점에 닿지 않아도 조금 더 넉넉하게 인식하여 쉽게 잡고 조작할 수 있도록 히트박스(Hitbox) 범위를 넓혔습니다.
- **상세 내용:** Fabric.js의 	ouchCornerSize 값을 기존 24에서 48로 2배 확장하여 꼭짓점 및 크기 조절 점들의 투명한 클릭 인식 범위를 크게 넓혔으며, 	argetFindTolerance 속성을 기존 15에서 25로 상향하여 개체 외곽선(변) 주변의 마우스 호버(hover) 및 선택 인식 범위를 더욱 여유롭게 개선했습니다.

#### 회전 마우스 커서 아이콘 크기 일관성 보장 (2026-06-11 추가)
- **개요:** 개체의 우측 상단 회전 꼭짓점을 잡았을 때 나타나는 회전 화살표 마우스 커서의 크기가 회전 꼭짓점 아이콘 크기와 일정하게 비례하도록 보장했습니다.
- **상세 내용:** 우측 상단 꼭짓점 제어 아이콘(파란 원)의 기본 크기를 20픽셀(표준 코너의 2배)로 확정하고, 마우스를 올렸을 때 나타나는 회전 화살표 커서(
awRotateSvg)의 크기를 정확히 꼭짓점 아이콘의 1.5배인 30x30 픽셀로 고정했습니다. 커서 핫스팟(중심점) 또한 15 15으로 맞춰 회전 시 마우스 중심이 튀지 않고 부드럽고 일관성 있게 표시되도록 수정했습니다.

#### 프로그램 초기 실행 화면 비율 및 중앙 배치, UI 크기 고정 (2026-06-11 추가)
- **개요:** 프로그램을 처음 실행할 때 창이 사용하는 모니터 해상도의 70% 크기로 일정하게 열리도록 보장하고, 화면 정중앙에 배치되도록 수정했습니다. 또한 고해상도(데스크톱 4K 등) 모니터에서 창 크기에 비례해 UI 버튼들이 비정상적으로 거대해지는 현상을 해결했습니다.
- **상세 내용:** main.py의 초기화 로직에서 main_window.move()를 활용해 수학적으로 화면 정중앙 좌표를 계산하여 창을 배치했습니다. 아울러 기존에 창 가로 픽셀(width) 너비에 따라 zoom_factor를 강제로 확대하던 로직을 제거하고 기본 줌을 1.0으로 고정하여, 노트북과 데스크톱 환경 모두에서 상단바 및 사이드바 버튼 크기가 일관되고 적절한 비율로 렌더링되도록 개선했습니다.

#### 인쇄 버튼 동작 방식 개선 (실제 하드웨어 프린터 지원) (2026-06-11 추가)
- **개요:** 기존에는 인쇄 미리보기(Print Preview) 창에서 '인쇄' 버튼을 눌렀을 때, 무조건 PDF 파일 저장 대화상자만 열리던 문제를 개선하여 컴퓨터에 연결된 실제 프린터들을 선택할 수 있도록 수정했습니다.
- **상세 내용:** main.py의 print_image 함수 내에서 QPrinter 객체에 setOutputFileName() 메서드가 하드코딩되어 강제로 '파일로 인쇄(Print to File)' 모드가 활성화되던 부분을 삭제했습니다. 대신 setDocName()으로 문서의 이름만 지정해 주어, 인쇄 미리보기 창에서 인쇄 버튼을 눌렀을 때 OS의 기본 프린터 선택 대화상자(하드웨어 프린터 및 PDF 인쇄 포함)가 정상적으로 호출되도록 시스템 네이티브 인쇄 기능을 완벽히 복구했습니다.

### 2026-06-11 선분/화살표 및 직선 펜 정밀 자석(Magnetic Snapping) 기능 추가
- **개요:** 선분, 화살표, 직선 펜 등 연결선을 그릴 때, 캔버스 상의 기존 개체(도형, 텍스트, 이미지 등)의 실제 시각적 테두리(Visual Boundary)에 완벽하게 안착(Snap)되도록 하는 고급 기능이 구현되었습니다.
- **주요 기능:**
  - **도형별 맞춤 곡선/사선 연산:** 직사각형의 4변뿐만 아니라, **타원**의 곡선 둘레 반경이나 **마름모**의 대각선(사선) 패스 위를 정확히 인식하여 거리가 15px 이내일 경우 정확히 달라붙습니다.
  - **시각적 인디케이터 제공:** 자석 기능이 활성화되어 테두리에 달라붙는 순간, 캔버스 위에 일시적인 반투명한 파란색 스냅 마커(인디케이터) 점이 렌더링되어 어디에 연결되었는지 사용자에게 직관적으로 알려줍니다.
  - **단축키 제어 (우회):** Alt 키를 누른 채로 선분을 그리면 자석 기능이 즉시 해제되어 주변 도형의 방해 없이 원하는 위치에 아주 미세하게 끝점을 지정할 수 있습니다.
- **성능 및 UX 최적화:**
  - 펜(자유곡선)과 모자이크와 같은 다량의 패스로 이루어진 복잡한 개체는 성능 저하를 방지하기 위해 최소 외곽 사각형(Bounding Box)을 기준으로 스냅하도록 최적화되었습니다.
  - 내부에 글상자가 적용된 도형(직사각형, 마름모, 타원)의 경우, 겹쳐 있는 내부 텍스트 객체에 중복으로 스냅되는 현상을 막고 바깥쪽 도형의 껍데기에만 깔끔하게 스냅되도록 처리되었습니다.

### 2026-06-12 개체 속성 수정 창의 배경 투명도 기본값 반영 및 수정 버그 해결
- **개요:** "펜" 및 "도형" 등으로 삽입된 개체의 '개체 속성 수정' 팝업을 열 때, 배경 투명도 기본값이 "환경설정"을 반영하도록 개선하고, 팝업창에서 조절한 배경 투명도가 실제 개체에 반영되지 않는 문제를 해결했습니다.
- **상세 내용:**
  - 기존에는 개체 속성 팝업 오픈 시 배경 투명도가 HTML에 고정된 값으로 표시되었으나, 해당 개체의 투명도(Alpha 채널)를 파싱하여 반영하고 값이 없을 경우 전역 "환경설정"의 투명도를 기본값으로 표시하도록 초기화 로직을 수정했습니다.
  - 팝업창 변경 내역 적용(`applyObjectEdit`) 시, 기존에는 창에서 입력한 투명도를 무시하고 전역 설정(`sysShapeOpacity`)을 강제로 덮어쓰던 로직 오류를 해결하여 사용자가 창에서 조절한 투명도 값이 개체에 정상적으로 즉시 렌더링되도록 개선했습니다.

### 2026-06-12 도형 내 글상자 삽입 시 텍스트 기본 색상 누락 버그 해결
- **개요:** "직사각형", "마름모", "타원" 도형에 "글상자로" 기능을 사용하여 텍스트를 입력할 때, 글자색이 전역 텍스트 색상 설정('오피스 빨강' 등)을 따르지 않고 무조건 검은색으로 나오던 현상을 수정했습니다.
- **상세 내용:**
  - `btn_add_text_to_shape` 이벤트 핸들러 내부에서 `window.textColor`를 참조하면서 스코프 문제로 인해 색상 값을 제대로 가져오지 못하고 기본값인 검은색(`#000000`)으로 대체되던 오류가 있었습니다.
  - 이를 로컬 스코프의 전역 변수인 `textColor`를 직접 참조하고, 만약 할당되지 않았을 경우 '오피스 빨강'(`#E03C31`)으로 기본 폴백(Fallback)하도록 수정하여 일관된 텍스트 입력 경험을 제공하도록 개선했습니다.

### 2026-06-12 투명도 조절창에서 0 입력 시 기본값으로 튕기는 버그 해결
- **개요:** 텍스트 배경 투명도, 형광펜 투명도 등의 입력창(또는 슬라이더)에서 투명도를 `0`으로 설정할 경우, `0`이 정상적으로 적용되지 않고 갑자기 `50` 또는 `30`과 같은 기본값으로 튕기며 강제 지정되는 오류를 수정했습니다.
- **상세 내용:**
  - 기존 코드에서는 `parseInt(this.value) || 50`과 같이 단축 평가(Short-circuit evaluation)를 사용하여 기본값을 설정하고 있었습니다.
  - 이로 인해 사용자가 명시적으로 `0`을 입력하더라도 자바스크립트에서 `0`을 Falsy(거짓) 값으로 평가하여 우측의 기본값(`50` 또는 `30`)을 반환해버리는 논리적 오류가 발생했습니다.
  - 이를 해결하기 위해 `isNaN(parseInt(this.value))` 함수를 사용하여, 입력값이 정말로 유효하지 않은 숫자(비어있거나 문자)일 때만 기본값으로 폴백(Fallback)하고 `0`은 유효한 숫자로 정상 처리하도록 텍스트 및 형광펜 투명도 로직들을 일괄 개선했습니다.

### 2026-06-12 텍스트 및 펜 설정 기본값 변경 및 배경 색상 지정 기능 활성화
- **개요:** 환경설정의 텍스트 설정과 펜 설정에서 편의성을 높이기 위해 배경 투명도의 기본값을 `15`로 조정하고, 펜 설정창 내 '배경 색상' 팔레트를 클릭할 수 없던 문제를 해결하여 투명/컬러를 지정할 수 있도록 수정했습니다.
- **상세 내용:**
  - 텍스트 설정의 '글자 색상' 기본값이 오피스 빨강, '입력창 배경색'이 투명임을 확인 및 보장하고, '입력창 투명도' 기본값을 `50`에서 `15`로 변경했습니다.
  - 펜 설정의 '배경 투명도' 기본값을 `0`에서 `15`로 변경하고, 내부 자바스크립트 변수인 `sysPenBgOpacity`의 기본값도 `15`로 동기화했습니다.
  - 환경설정 팝업에서 펜 배경색(`set_pen_bg_color`)을 클릭해도 컬러 팔레트가 열리지 않던 오류를 해결하기 위해 컬러 선택기 이벤트 바인딩(`bindCP`)에 해당 요소를 추가했습니다.
  - 설정 저장(`collectSettings`) 및 불러오기/적용(`applySettings`) 사이클에서 펜 배경색과 텍스트 배경색이 누락되던 점을 발견하여 이를 온전히 저장하고 복원할 수 있도록 로직을 함께 보완했습니다.
  - 추가로, 환경설정에서 지정한 펜의 배경 색상(`sysPenBgColor`)과 투명도(`sysPenBgOpacity`)가 실제 자유 드로잉으로 생성되는 펜 개체 및 자동 인식 도형에 실시간으로 연동되어 속성값으로 채워지도록(`fill`) `path:created` 이벤트 및 `_createRecognizedShape` 함수의 매개변수 구조를 개선했습니다.
  - **버그 추가 수정:** 이전에 잘못된 배경 이미지(체크무늬)가 설정 파일에 저장된 채로 남아있을 경우, 앱을 재실행할 때(`applySettings`) 과거의 찌꺼기 설정값이 덮어씌워져 환경설정 팝업과 툴바(토글창)의 색상표 원형이 여전히 깨져 보이는 문제가 지속되었습니다. 이를 완전히 해결하기 위해 색상을 불러오거나 적용할 때 투명도가 아닌 단색이라면 강제로 `background-image`를 `'none'`으로 초기화(Sanitization)하도록 일괄 보완했습니다.

### 2026-06-12 펜 및 도형 토글창 속성(투명도 등) 연동 및 실시간 반영 개선
- **개요:** "환경설정"의 기본값과 "토글창(서브 툴바)"의 입력값, 그리고 "실제 그려지는 개체" 사이의 값이 동기화되지 않고 각자 따로 놀던 문제를 해결했습니다.
- **상세 내용:**
  - **환경설정 → 토글창 동기화:** `applySettings` 함수 내부에서 `set_shape_opacity_val`(환경설정 도형 투명도) 및 `set_pen_bg_opacity`(환경설정 펜 투명도) 값을 불러와, 메인 화면의 토글창에 있는 `shape_bg_opacity_input` 및 `pen_bg_opacity_input` 텍스트 입력칸에 일괄 동기화시켜 덮어쓰도록 수정했습니다.
  - **토글창 → 캔버스 개체 실시간 반영:** 기존 코드에서는 펜이나 도형을 삽입할 때 `sysPenBgOpacity`나 `sysShapeOpacity` 같은 시스템 내부 변수를 직접 참조하여 렌더링하고 있었습니다. 이로 인해 토글창에서 아무리 투명도를 바꿔도 개체에 반영되지 않았습니다.
  - 이를 해결하기 위해 개체 생성 시점(`path:created` 이벤트 및 `getFillOpacity()` 함수)에서 내부 변수보다 토글창의 `pen_bg_opacity_input` 및 `shape_bg_opacity_input` 값을 먼저 읽어오도록(`우선순위 역전`) 수정했습니다.
  - 입력값이 비정상적(`isNaN`)일 때만 시스템 기본값으로 폴백하도록 처리하여, 이제 토글창에서 투명도 숫자를 수정하고 캔버스에 그리면 즉각적으로 해당 투명도가 반영됩니다.
  - **형광펜 농도 추가 동기화:** 펜이 '형광펜' 모드일 때만 나타나는 '형광펜 농도(`pen_highlighter_intensity_input`)' 토글창 입력값도, 동일한 원리로 환경설정 기본값(`set_pen_highlighter_opacity`)에서 불러오도록 `applySettings`에 동기화 로직을 추가했습니다.
  - 또한 해당 토글창에서 농도를 변경하는 즉시 `penCurrentColor`의 알파(Alpha) 채널에 투명도를 실시간으로 재계산하여 반영하는 이벤트 리스너를 추가했습니다.
### 2026-06-12 캔버스 이미지 드래그 삽입 시 원본 비율(Aspect Ratio) 강제 유지 기능 추가
- **개요:** 기존에 한 번 업로드했던 이미지를 상단바의 "이미지" 버튼을 통해 캔버스에 드래그하여 재삽입할 때, 사용자가 마우스를 드래그하는 각도에 따라 이미지가 가로/세로로 마음대로 찌그러지며 왜곡되는 현상을 방지했습니다.
- **상세 내용:** 
  - `mouse:move` 이벤트 핸들러 내부에서 `activeTool === 'image'`일 때 실행되는 이미지 리사이징 로직을 개선했습니다.
  - 기존에는 원형/정사각형 자르기 모드(`frame === 'square' || 'circle'`)일 때만 비율이 유지되고, 원본 삽입 시에는 가로 드래그와 세로 드래그 길이를 각각 별도의 스케일(`scaleX`, `scaleY`)로 적용하여 이미지가 왜곡되었습니다.
  - 이를 마우스 드래그의 가로/세로 거리 중 더 크게 이동한 값을 기준으로 통합된 `scale` 값을 계산한 뒤, `scaleX`와 `scaleY`에 동일하게 부여하여 **드래그 각도와 상관없이 항상 이미지의 원본 비율이 유지된 채로 크기만 확대/축소되도록** 변경했습니다.

### 2026-06-12 선분 및 화살표 일반(클릭&드래그) 삽입 기능 분리
- **개요:** 기존에 다중 클릭(더블클릭 또는 ESC로 완료) 방식으로만 삽입되던 '선분'과 '화살표' 도형 중, '일반' 모드로 삽입할 때의 조작 방식을 간단한 클릭&드래그로 변경했습니다.
- **상세 내역:**
  - '도형' 도구에서 '선분' 또는 '화살표'를 선택하고 선 유형을 '일반'으로 설정한 경우, 마우스를 클릭하고 드래그한 후 떼면(mouse:down -> mouse:move -> mouse:up) 즉시 선이 완성됩니다.
  - 더 이상 완료를 위해 더블클릭이나 ESC를 누를 필요가 없어 작업 속도가 향상되었습니다.
  - 단, '꺾은 선(Polyline)'이나 '곡선(Curve)' 모드일 때는 기존과 동일하게 다중 클릭 방식을 유지하여 여러 번 꺾이거나 휘어지는 선을 계속 그릴 수 있도록 보장했습니다.

### 2026-06-13 이미지 자르기 기능 고도화 (영역 확장 및 자석 스냅 자르기)
- **개요:** 단순 축소만 가능했던 기존 자르기 기능을 개선하여 캔버스 배경 밖으로 영역을 확장할 수 있도록 하고, UI 요소 주변으로 쉽게 달라붙는 자석 스냅(Edge-Snapping) 기능을 추가했습니다.
- **상세 내역:**
  - **영역 확장 기능:** 자르기 오버레이의 네 변을 잡고 바깥으로 드래그하면 원본 이미지 크기를 넘어 영역을 확장할 수 있습니다. 이때 확장된 여백은 확장된 방향의 변 픽셀 최빈값(Most Common Color)을 자동으로 추출하여 위화감 없이 동일한 색상으로 채워집니다.
  - **컴포넌트 경계 자석 자르기:** 마우스를 드래그할 때 커서 반경 20px 영역 내 픽셀을 스캔(Sobel 유사 필터 활용)하여, 픽셀 색상 차이가 극명하게 갈리는 경계선(버튼, 다이얼로그 등)을 감지합니다. 경계선이 반경 15px 내에 포착되면 자르기 선이 자동으로 자석처럼 달라붙어, 1픽셀 단위의 미세 조정 없이도 깔끔하게 UI 요소를 자를 수 있습니다. (Alt 키를 누르고 드래그 시 자석 기능 무시 가능)

### 2026-06-13 이미지 자르기 안내 문구 변경 및 변 인식 영역(Hit area) 확장
- **개요:** 자르기 안내 문구를 보다 직관적으로 변경하고, 자르기 선 주변의 클릭 인식(Hit area) 영역을 늘려 사용성을 크게 개선했습니다.
- **상세 내역:**
  - "이미지의 네 변을 조절하여 자를 수 있습니다."라는 문구를 "이미지의 네 변을 조절하여 영역을 축소 또는 확장할 수 있습니다."로 변경했습니다.
  - "이 모양대로 자르기" 버튼의 텍스트를 "확인"으로 간결하게 변경했습니다.
  - 마우스를 정확히 자르기 얇은 선(4px)에 올리지 않아도, 근처 상하/좌우 5px(총 14px 영역) 범위에만 접근하면 자르기 조절 모드로 쉽게 진입할 수 있도록 CSS `padding` 확장 및 JS `top/left` 오프셋 보정 로직을 도입했습니다. 시각적인 선 굵기는 그대로 유지하면서 조작 편의성만 대폭 향상되었습니다.

### 2026-06-13 3D 팝아웃 기능 입체감 개선 및 글상자 UI 버그 수정
- **개요:** 팝아웃 생성 시 기본 테두리와 그림자 효과를 강화하고, 회전 로직을 원근감이 느껴지는 진정한 3D 틸트 방식으로 변경했으며, 사라졌던 도형의 글상자 추가 버튼을 복구했습니다.
- **상세 내역:**
  - **입체 효과 강화:** 생성 시 기본 테두리를 4px에서 8px로 두껍게 변경하고, 그림자의 흐림(Blur) 수치를 50, 수직 오프셋을 25로 대폭 늘려 바닥에서 더 높이 떠 있는 듯한 확실한 입체감을 부여했습니다. 초기 생성 시 존재하던 비대칭 기울기(Skew)는 제거하여 깔끔하게 정면을 바라보게 초기화했습니다.
  - **가로/세로 3D 회전(Tilt) 변경:** 2D 평면에서 시각적으로만 기울어지는 단순 Skew 방식을 버리고, X축/Y축 중심의 실제 3D 회전(원근감)을 묘사하는 방식으로 변경했습니다. 
    - 가로 중심축 회전 시: Y축 스케일(높이)이 압축되면서 그림자가 위아래로 크게 이동하여, 윗부분과 아랫부분이 번갈아 앞뒤로 기울어지는 효과를 줍니다.
    - 세로 중심축 회전 시: X축 스케일(너비)이 압축되면서 그림자가 좌우로 크게 이동하여, 왼쪽과 오른쪽이 번갈아 앞뒤로 기울어지는 효과를 줍니다.
  - **글상자 버튼 복구:** 팝아웃 아이콘 추가 시, 도형(직사각형, 타원, 마름모)의 글상자 아이콘(addText)과 위치(`offsetX`)가 겹쳐서 가려지던 버그를 발견하고, 팝아웃 아이콘의 `offsetX`를 150으로 밀어내어 두 버튼이 모두 정상적으로 보이도록 복원했습니다.

### 2026-06-13 3D 팝아웃 기능 마이너 피드백 반영
- **개요:** 3D 팝아웃 기능 사용 시 사용자 피드백에 따라 자잘한 UX 및 렌더링 버그를 해결했습니다.
- **상세 내역:**
  - **연결선 굵기 보강:** 팝아웃 개체와 원본 개체 사이를 이어주는 연결선의 굵기를 2px에서 4px로 두껍게 수정하여 시인성을 높였습니다.
  - **제어 마커 버그 수정:** 팝아웃 생성 시 기본적으로 컨트롤 마커가 보이지 않도록 `hasControls: false`, `hasBorders: false`를 적용했습니다. 또한 다른 오브젝트 클릭 해제 시 간헐적으로 남아있던 찌꺼기 마커를 완전히 제거하기 위해 `selection:cleared` 이벤트 발생 시 캔버스의 전체 오브젝트들을 한 번 더 검사하여 강제로 마커를 숨기는 강력한 방어 로직을 추가했습니다.
  - **3D 원근감 렌더링 정상화 (오브젝트 캐싱 해제):** 팝아웃 개체가 회전하지 않고 뒤에 그림자만 움직이던 치명적인 렌더링 버그를 수정했습니다. Fabric.js의 기본 캐시 정책(`objectCaching`)이 켜져 있으면 커스텀 렌더링(`_render` 오버라이딩을 통한 스트립 슬라이싱) 결과물이 캐시 범위를 넘어가 잘리거나 무시되는 현상을 발견하고, 팝아웃 개체에 한해 `objectCaching: false` 속성을 부여하여 매 프레임마다 실제 왜곡된 3D 픽셀을 정상적으로 그려내도록 처리했습니다.
  - **슬라이더 UI 직관성 개선:** 기존의 가로형 레인지(Range) 슬라이더 바와 플로팅 텍스트(span) 조합을 제거하고, 키보드로 직접 숫자를 입력하거나 상/하 화살표 버튼을 클릭해 각도를 미세하게 조절할 수 있는 업다운 입력창(Number Input)으로 전면 교체하여 조작 편의성을 크게 높였습니다.

### 2026-06-13 3D 팝아웃 기능 고급 렌더링 도입 및 버그 핫픽스
- **개요:** 팝아웃 확장 속성 UI(테두리, 그림자 등) 연동 누락 오류, 투명한 배경 크롭 시 그림자가 앞으로 비쳐보이는 오류, 선택 해제 마커 버그 등 핵심 사용성 버그를 일괄 수정하고 리얼 3D 렌더링을 도입했습니다.
- **상세 내역:**
  - **투패스 렌더링(Two-Pass Slicing) 적용:** 오프스크린 캔버스를 활용하여 가로축(Tilt X) 왜곡과 세로축(Tilt Y) 왜곡을 교차 렌더링하는 듀얼 축 4점 원근 투영법을 성공적으로 구현하여 가로/세로 동시 회전이 가능해졌습니다.
  - **제어 마커 버그 완벽 차단:** 선택 시 컨트롤 마커가 보이지 않고, 선택 해제 시 잔류 마커가 보이던 오작동을 해결하기 위해 팝아웃도 정상적인 `fabric` 개체 선택(`selection:cleared` 로직) 흐름을 타게 복구하여 버그를 차단했습니다.
  - **크롭 위치 좌표계 불일치 수정:** `createPopoutFromObject` 내부에서 줌인/아웃 뷰포트 상태일 때 엉뚱한 우측 하단이 캡처되던 버그를 뷰포트 원복 로직 순서 변경을 통해 고쳤습니다.
  - **반투명 그림자 간섭(투과) 차단:** 내부 텍스트 주변 등 배경이 투명하게 크롭될 경우, 후면 그림자가 앞쪽으로 비쳐 지저분하게 보이던(그림자가 앞에 생기는) 오류를 방지하기 위해 단색 흰색 베이스(`source-in`)를 먼저 칠하고 이미지를 합성하도록 렌더링 파이프라인을 개선했습니다.
  - **확장 UI 패널 수치 동기화 완벽 연결:** 연결선 표시/색상/두께 및 테두리, 그림자 강조 수치 조절기가 런타임에 즉시 반응하도록 정규식 인젝션 로직을 보완하고 `obj.dirty = true` 속성을 부여하여 렌더링 성능과 즉각적인 피드백을 동시에 달성했습니다.


### 팝아웃(Popout) 개체 개선
- **내부 영역 마스킹 정확도 개선**: `regeneratePopoutBase`에서 팝아웃 개체를 렌더링할 때 원본 도형의 `originX`/`originY`를 임시로 `center`로 조정하여 중심축이 일치하지 않아 잘려나온 이미지가 어긋나는 현상 수정.
- **회전 시 그림자 중첩 및 왜곡 방지**: 팝아웃 개체를 가로축(Tilt X) 및 세로축(Tilt Y)으로 회전할 때, 내부적으로 스트립 단위 스캔라인 렌더링 과정에서 그림자가 지속 중첩되어 오브젝트 앞쪽으로 튀어나오는 오류 수정. 이제 그림자는 최종 형태 렌더링 완료 시 단 한 번, 개체 뒤쪽에만 정확히 표시되도록 파이프라인(`_render`) 변경.
- **테두리 외곽 형성 및 치우침 현상 해결**: 팝아웃 테두리를 그릴 때 기존 방식(`source-over` 위에 테두리 그림)에서 다중 캔버스를 이용한 밑바탕(Outer stroke) 기법으로 변경. 테두리 두께를 2배로 그린 뒤 위에 내용물을 덮어씌움으로써 완벽하게 원개체 경계선 바깥(외곽)으로만 테두리가 형성되도록 개선.


### 환경 설정 - 팝 아웃 설정 기능 추가
- **UI 위치**: '환경 설정' 창의 '도형 설정' 아래에 '팝 아웃 설정' 섹션을 추가.
- **기본값 지정 기능**: 팝아웃 개체를 생성할 때 환경 설정에서 지정한 기본 수치들이 자동으로 적용됨.
  - **연결선 표시**: 체크박스 (기본값 ON)
  - **연결선 색상**: 42가지 색상 선택 지원 (기본값: 진회색)
  - **연결선 두께**: 수치 1 ~ 20 (기본값: 4)
  - **개체 기본 회전**: 팝아웃 생성 시 자동으로 개체가 회전됨 (수치 0.0 ~ 359.9, 기본값: 10.0)
  - **가로축 회전 (Tilt X)**: 수치 -45 ~ 45 (기본값: 20)
  - **세로축 회전 (Tilt Y)**: 수치 -45 ~ 45 (기본값: -30)
  - **테두리 색상**: 42가지 색상 선택 지원 (기본값: 하양)
  - **테두리 두께**: 수치 0 ~ 30 (기본값: 10)
  - **그림자 색상**: 42가지 색상 선택 지원 (기본값: 검정)
  - **그림자 강도**: 수치 0 ~ 100 (기본값: 50)


### 팝아웃(Popout) 개체 조작성 및 속성창 UI 개선
- **외곽선(Bounding Box) 크기 최적화**: 팝아웃 개체 생성 시 여백(Padding)을 최소화하여(40px -> 10px) 선택 외곽선 점선이 개체 크기에 딱 맞게 표시되도록 개선함.
- **상단 속성 토글창 자동 표시 및 배치 개선**: 팝아웃 개체를 클릭(선택)하면 즉시 상단 툴바 아래에 팝아웃 전용 속성 토글창이 뜨도록 처리함.
  - 순서: 연결선, 선색, 선 두께, 가로축 회전, 세로축 회전, 테두리 색상, 테두리 두께, 그림자 색상, 그림자 강도
- **개체 속성 수정 창(더블클릭) 내부 팝아웃 레이아웃 추가**: 팝아웃 개체 전용 폼이 4줄로 정돈되어 출력되도록 개선.
  - 1줄: 크기(가로, 세로), 비율 유지(체크박스)
  - 2줄: 연결선 표시(체크박스), 연결선 색상, 연결선 두께
  - 3줄: 가로축 회전, 세로축 회전
  - 4줄: 테두리 색상, 테두리 두께, 그림자 색상, 그림자 강도
  - 창이 열릴 때 객체에 지정된 값이 있으면 해당 값을 보여주고, 없으면 환경설정 값을 기반으로 출력함.
- **환경설정 UI 개선**: 팝 아웃 설정 섹션 내 수치 입력창(테두리 두께, 그림자 강도 등)의 너비를 다른 항목과 동일하게 넓혀 수치 가독성을 개선함.


### 팝아웃(Popout) 개체 UI 오류 및 편의성 2차 수정 (2026-06)
- **상단 속성 토글창 연동 오류 수정**: 팝아웃 개체를 클릭할 때 간헐적으로 툴바의 다른 이벤트가 패널을 가리던 문제를 근본적으로 해결하여, 캔버스에서 개체를 선택하면 즉시 팝아웃 전용 속성 토글창이 안정적으로 팝업되도록 보완함.
- **외곽선(Bounding Box) 완벽 밀착**: 팝아웃 크롭 여백(Pad)을 0으로 완전히 제거하여, 선택 시 외곽선 점선이 개체의 크기와 형태에 오차 없이 딱 맞물리도록 최적화함.
- **42가지 컬러 피커(Color Picker) 연동**: 팝아웃 토글창 내부의 연결선 색상, 테두리 색상, 그림자 색상 버튼 클릭 시 프로그램 고유의 42가지 색상표 팔레트가 열리며, 색상 선택 시 실시간으로 팝아웃 객체에 적용되도록 이벤트를 바인딩함.
- **속성 수정 모달(더블클릭) 레이아웃 재적용**: 이전 업데이트에서 기존 코드의 형태 차이로 인해 반영되지 못했던 팝아웃 전용 속성 수정 폼(크기/비율유지, 연결선, 회전, 테두리/그림자의 4줄 구성)을 정규식 매핑으로 정확히 주입하여 해결함.

## 팝아웃 기능 추가 수정 및 안정화 (2026-06)
- **팝아웃 속성창 즉시 표시:** 캔버스에서 팝아웃 개체를 클릭하면 상단 툴바 바로 아래에 속성 토글창(`sub_toolbar`)이 즉시 위치하도록 수정.
- **팝아웃 회전/테두리 최적화 (외곽선 핏 보정):** 가로/세로축 회전(`tiltX`, `tiltY`) 적용 시 실제 객체에 `skewX`, `skewY` 속성이 누락되던 문제를 해결하여 시각적 왜곡과 선택 외곽선 불일치 현상 수정. 또한 `regeneratePopoutBase` 내에서 테두리 굵기(`borderW`)만큼 캔버스 크기를 능동적으로 확장하여, 외곽선이 캔버스 경계에 잘리지 않고 깔끔하게 맞도록 최적화.
- **팝아웃 연결선 색상 팔레트 오류 수정:** 연결선 색상 변경 시 기본 OS 컬러 피커가 중복 호출되는 이벤트 리스너를 삭제하고, 내부 42색 컬러 팔레트와 정상 연동되도록 수정.
- **개체 속성 수정 창의 색상 변경 실시간 반영:** `applyObjectEdit` 함수 내에 팝아웃 전용 처리 루틴을 보완하여, 테두리 색상 및 그림자 색상이 수정 즉시 캔버스(`regeneratePopoutBase`)에 반영되도록 개선.

## 팝아웃 기능 3D 원근감 렌더링 복원 및 UI 개선 (2026-06)
- **팝아웃 3D 원근감 복원 및 외곽선 정밀 매칭:** 이전 수정에서 `skewX`/`skewY`를 사용하여 외곽선을 맞추려다 손실된 3D 원근감(가까운 쪽은 커지고 먼 쪽은 작아지는 원근 효과)을 `regeneratePopoutBase` 내부에 투영 렌더링을 직접 베이킹하는 방식으로 완벽히 복원함. 이를 통해 3D 원근감이 살아있으면서도 Fabric.js의 선택 외곽선(bounding box)이 투영된 최종 이미지의 크기와 한 치의 오차도 없이 딱 들어맞도록 구현함.
- **팝아웃 속성 토글창(sub_toolbar) 위치 정렬:** 토글창이 캔버스 중앙(`left: 50%`)에 치우쳐 있던 문제를 수정하여, 상단바의 "텍스트" 버튼(`btn_text`)의 왼쪽 라인과 일치하도록 정렬 기준 위치를 변경함.
- **팝아웃 테두리 두께 변경 오류 수정:** 속성창에서 테두리 두께(`popout_border`)를 변경할 때 캔버스에 즉각 반영되지 않던 문제를 `regeneratePopoutBase` 이벤트 트리거 콜백 함수를 정상적으로 연결하여 실시간으로 반영되도록 해결함.

## 팝아웃 기능 관련 2차 안정화 및 버그 픽스 (2026-06)
- **생성 시 기본값 미반영 문제 해결:** 팝아웃 객체를 최초 생성할 때 환경설정에서 지정한 기본값(가로축/세로축 회전값 등)이 비정상적으로 적용되던 문제를 수정. `skewX/Y` 속성을 0으로 초기화하고, 생성 즉시 3D 원근감 렌더링 로직(`applyPopoutTilt`)을 호출하여 기본 환경설정값이 즉시 반영되도록 구현함.
- **속성창 UI 위치 오정렬 완벽 해결:** 팝아웃 속성창이 상단바의 텍스트 버튼 왼쪽에 맞춰지지 않던 현상을 텍스트 버튼의 정확한 ID(`btn_tool_text`)를 참조하도록 수정하여 UI 정렬 문제를 완벽히 해결함.
- **속성창 값 변경 시 즉각 반영되도록 수정:** 속성창에서 슬라이더나 수치 변경 시 적용 버튼을 누르지 않아도 개체에 바로 렌더링이 반영되도록 `applyPopoutTilt` 함수 끝단에 `regeneratePopoutBase` 호출을 명시적으로 추가하여 실시간 양방향 바인딩을 구현함.

## 팝아웃 개체 기본값 설정 변경 (2026-06)
- **기본값 업데이트:** "환경설정" UI 및 팝아웃 객체 생성, 속성 토글창 등 모든 팝아웃 관련 로직에서 사용되는 '개체 기본 회전'의 초기값을 기존 `10.0`에서 `8.0`으로, '테두리 두께'의 초기값을 기존 `10`에서 `8`로 일괄 변경함.

## 팝아웃 개체 직렬화/역직렬화(Undo/Redo 및 자동저장) 복구 (2026-06)
- **팝아웃 속성 보존:** Fabric.js의 기본 객체 직렬화(`toObject`) 시 누락되던 팝아웃 전용 커스텀 속성들(`isPopout`, `tiltX`, `tiltY`, `popoutBorder` 등)을 직렬화 배열에 명시적으로 추가하여, Undo/Redo 시 데이터가 증발하지 않도록 수정함.
- **참조 객체 연결(Link) 복원:** 팝아웃 개체와 그 모체인 원본 도형(`linkedOriginal`), 연결선(`linkedLine`) 간의 양방향 연결을 유지하기 위해 생성 시 고유 ID를 부여하여 직렬화에 포함함. 불러오기 시(`finalizeLoad`) ID를 대조하여 잃어버린 연결 고리를 자동으로 재맵핑하는 로직 구현.
- **3D 렌더링 상태 복구:** 팝아웃의 기본 크롭 이미지(`_croppedBg`)를 DataURL 형태로 백업 저장하고, 불러올 때 이를 읽어 임시 캔버스(Canvas) 객체로 복원한 뒤 `applyPopoutTilt`를 실행시켜 저장 당시의 사실적인 3D 렌더링을 완전히 동일하게 복구하도록 구현.

## 15. 팝아웃 개체 관련 주요 규칙
- 팝아웃 개체 속성(회전, 기울기, 테두리, 그림자 등)은 JSON 직렬화에 포함되어야 Undo/Redo 및 프로그램 재시작 시 상태가 유지됩니다 (customProps에 등록).
- 팝아웃의 원근감은 
egeneratePopoutBase를 통한 사다리꼴 변형으로 구현하며, FabricJS의 네이티브 skewX/skewY를 직접 설정하면 변형 충돌로 인해 원근감이 깨지므로 사용을 금지합니다 (obj.set({skewX: 0, skewY: 0})로 초기화).
- 팝아웃 개체 선택 시 상단바(sub_toolbar) 및 팝아웃 설정 패널(panel_popout)이 자동으로 표시되어 즉각적인 속성 수정이 가능해야 합니다 (selection:created 및 selection:updated에 처리).
- 팝아웃 생성 시 기본값(기본 회전 8.0, 테두리 두께 8 등)이 유실되지 않도록 pplySettings에서 빈 값을 방어합니다.

- **팝아웃 Undo/Redo 버그 수정:** 팝아웃 개체를 복원하는 popoutReviver 함수의 스코프가 commitLoadToCanvas 내부에 갇혀 있어 뒤로 가기/앞으로 가기 시 ReferenceError가 발생하던 문제를 해결. 함수를 글로벌 스코프로 이동하여 안정적인 Undo/Redo 지원.
- **환경설정 기본값 적용 강제화:** 사용자의 로컬 환경설정 캐시(.capcraft_settings.json)에 이전 값(예: 테두리 10)이 저장되어 있어 새 기본값(8.0, 8)이 무시되던 현상을 파악하여, 스크립트 단에서 해당 캐시를 8.0과 8로 강제 리셋하여 기본값이 정상적으로 반영되도록 조치.

- **팝아웃 Undo/Redo 배경 복구 및 커스텀 속성 직렬화 개선:** JSON 복원 시 `_croppedBgDataUrl`를 비동기적으로 로드한 뒤 팝아웃 원근감/그림자 상태(`regeneratePopoutBase`)를 다시 트리거하도록 안전하게 개선하여, 히스토리(Undo/Redo) 탐색이나 프로그램 재시작 시 팝아웃 개체가 사라지던 치명적인 문제 완전 해결.

### 2026-06-14 팝아웃 Undo/Redo 완전 복원 및 그림자 강도 실시간 반영 수정

#### 팝아웃 Undo/Redo 시 개체 소실 문제 완전 수정
- **근본 원인 1 — 객체 참조 복원 누락:** `loadFromJSON` + `popoutReviver`로 팝아웃의 JSON 속성(`linkedOriginalId`, `linkedLineId`, `popoutShadow` 등)은 복원되지만, 런타임 객체 참조(`linkedOriginal`, `linkedLine`)는 복원되지 않았음. 이로 인해 `updatePopoutLine`이나 `regeneratePopoutBase`가 참조를 찾지 못해 팝아웃의 연결선이 사라지고 그림자/기울기 렌더링이 실패함.
- **근본 원인 2 — `isPopoutLine` 직렬화 누락:** 팝아웃 연결선 객체의 `isPopoutLine` 속성이 `toObject` 및 `toJSON` 커스텀 속성 목록에 등록되지 않아, JSON 직렬화 시 연결선을 식별할 수 없었음.
- **근본 원인 3 — `object:removed` 연쇄 삭제:** `loadFromJSON` 내부의 `canvas.clear()`가 기존 객체를 제거할 때 `object:removed` 이벤트가 발생하고, 이 핸들러가 팝아웃의 `linkedLine`을 연쇄 삭제하여 복원 대상 객체까지 제거됨.
- **수정 내역:**
  1. `restorePopoutLinks()` 함수 신규 추가: `loadFromJSON` 콜백에서 `restoreLinkedTexts()` 이후 호출되어, 모든 `isPopout` 객체의 `linkedOriginalId`와 `linkedLineId`를 ID Map 기반으로 대조하여 `linkedOriginal`/`linkedLine` 런타임 참조를 완벽히 복원하고, `applyPopoutTilt()` 및 `updatePopoutLine()`을 재실행하여 그림자/연결선 상태를 재구성.
  2. `isPopoutLine` 속성을 `fabric.Object.prototype.toObject` 직렬화 목록과 모든 `canvas.toJSON()` 호출에 추가하여 연결선 객체가 JSON에 포함되도록 수정.
  3. `popoutReviver`에 `isPopoutLine` 복원 로직 추가하여 역직렬화 시 연결선 속성이 올바르게 복원되도록 개선.
  4. `canvas.on('object:removed')` 핸들러에 `isHistoryAction` 가드 추가하여 Undo/Redo 중 `canvas.clear()` 트리거 시 팝아웃 연결선의 연쇄 삭제를 방지.

#### 팝아웃 그림자 강도 실시간 반영 수정
- **근본 원인:** `bindPopoutInput('popout_shadow', ...)` 콜백이 `regeneratePopoutBase`로만 바인딩되어 있었음. 실제 shadow 객체의 `blur`, `offsetX`, `offsetY`, `color`를 설정하는 함수는 `applyPopoutTilt`이며, `regeneratePopoutBase`는 이미지 합성만 담당하여 그림자 변경이 반영되지 않았음.
- **수정:** `popout_shadow` 입력의 콜백을 `applyPopoutTilt`로 변경. `applyPopoutTilt` 함수 내부에서 shadow 속성 설정 후 `regeneratePopoutBase(obj)`를 이미 호출하고 있으므로 두 기능 모두 정상 동작.

### 2026-06-14 팝아웃 Undo/Redo 위치 편차(Position Drift) 및 개체 소실 근본 수정

#### 근본 원인 1 — `_baseLeft`/`_baseTop` 직렬화 누락으로 인한 위치 누적 편차
- **문제:** `regeneratePopoutBase` 함수는 3D 투영(tiltX/tiltY) 적용 후 투명 영역을 잘라내면서 팝아웃의 `left`/`top` 위치를 `_baseLeft + shiftX`로 보정함. 그런데 `_baseLeft`와 `_baseTop`은 직렬화(toObject/toJSON) 속성 목록에 포함되지 않아 Undo/Redo 시 `loadFromJSON`으로 복원된 팝아웃에는 `_baseLeft`가 `undefined`임.
- **증상:** `regeneratePopoutBase`가 역직렬화 후 호출될 때 `_baseLeft === undefined`이므로 이미 shift가 포함된 `left` 값(= 원래 `_baseLeft + shiftX`)을 `_baseLeft`로 설정 → 다시 `left = _baseLeft + shiftX` = `원래_baseLeft + 2×shiftX`로 계산됨. 매번 Undo/Redo를 실행할 때마다 shift가 누적 적용되어 팝아웃이 점점 화면 밖으로 밀려나가 "사라지는" 것처럼 보임.
- **수정:** `_baseLeft`와 `_baseTop`을 다음 7곳에 모두 추가하여 직렬화/역직렬화 시 보존되도록 함:
  1. `fabric.Object.prototype.toObject` 커스텀 속성 목록
  2. `pushCurrentToRecent()` 내 `canvas.toJSON()` 호출
  3. `initHistory()` 내 `canvas.toJSON()` 호출
  4. `saveHistory()` 내 `canvas.toJSON()` 호출
  5. JSON 파일 저장 시 `canvas.toJSON()` 호출
  6. `popoutReviver` 속성 복원 목록
  7. `_baseLeft`/`_baseTop`이 직렬화되므로 역직렬화 후 `regeneratePopoutBase`가 올바른 기준 좌표를 사용하여 위치 편차 완전 제거

#### 근본 원인 2 — `_croppedBg` 비동기 로드와 `linkedOriginal` 복원 간의 타이밍 충돌
- **문제:** `popoutReviver`의 `img.onload` 콜백과 `restorePopoutLinks()` 함수가 서로 의존하는 데이터를 비동기적으로 설정하여 경합 조건(race condition) 발생:
  - `popoutReviver`는 `_croppedBgDataUrl`에서 `Image`를 **비동기** 로드 → `onload`에서 `_croppedBg` 설정 후 `regeneratePopoutBase` 호출 → 이 시점에 `linkedOriginal`이 아직 복원 안 됨(`restorePopoutLinks`가 아직 실행 전) → `regeneratePopoutBase`에서 `if (!obj) return;`으로 빠져 렌더링 실패
  - `restorePopoutLinks`에서 `linkedOriginal` 복원 후 `applyPopoutTilt` 호출 → `regeneratePopoutBase` → `if (!img._croppedBg) return;`으로 빠져 렌더링 실패 (이미지 아직 로드 중)
  - 두 경로 모두 실패하여 팝아웃이 빈 이미지 상태로 남음
- **수정 — 양방향 조건 체크 패턴 도입:**
  - `restorePopoutLinks()`: `applyPopoutTilt(o)`를 `o._croppedBg`가 이미 로드된 경우에만 호출. 미로드 시에는 `popoutReviver`의 `img.onload`에 위임.
  - `popoutReviver`의 `img.onload`: `regeneratePopoutBase` 대신 `obj.linkedOriginal`이 복원된 경우에만 `applyPopoutTilt` + `updatePopoutLine` 호출. 미복원 시에는 `restorePopoutLinks`에 위임.
  - 어느 쪽이 먼저 실행되든 나머지 한쪽에서 완전한 복원을 보장하는 교차 확인 구조.

#### 근본 원인 3 — `_originalElement` 미동기화로 인한 팝아웃 투명 직렬화
- **문제:** `fabric.Image`의 `toObject()`는 `getSrc()` → `_originalElement.toDataURL()` 또는 `_originalElement.src`로 `src` 속성을 직렬화함. 팝아웃 생성 시 `fabric.Image.fromURL(dummyCanvas.toDataURL(), ...)`로 **투명한 더미 HTMLImageElement**가 `_originalElement`에 설정됨. 이후 `regeneratePopoutBase`에서 `img._element = trimmedCanvas`로 실제 렌더링 캔버스를 교체하지만, `_originalElement`는 여전히 투명 더미를 가리킴.
- **증상:** `saveHistory()` → `canvas.toJSON()` → popout의 `src`가 투명 이미지 data URL로 직렬화됨. `loadFromJSON`이 이 `src`로 `fabric.Image`를 복원하면 **시각적으로 아무것도 보이지 않는 투명 이미지**가 생성됨. 비동기 복원 파이프라인(`popoutReviver` + `restorePopoutLinks`)이 완전히 성공해야만 보이게 되지만, 하나라도 실패하면 팝아웃이 영구 소실됨.
- **수정:** `regeneratePopoutBase` 함수 내에서 `img._element`를 교체하는 **모든 경로**(trim 불필요 시 `finalCanvas`, 정상 경로 `trimmedCanvas`)에서 `img._originalElement`도 함께 동일한 캔버스로 업데이트. 이로써 `toObject()` → `getSrc()` → `_originalElement.toDataURL()`이 실제 렌더링된 팝아웃 이미지를 반환하게 되어, `loadFromJSON`이 즉시 올바른 시각적 내용을 가진 `fabric.Image`를 생성함.
- **검증:** `fabric.min.js`의 `getSrc` 구현을 직접 확인: `getSrc:function(t){t=t?this._element:this._originalElement;return t?t.toDataURL?t.toDataURL():...}` — 인수 없이 호출 시 `_originalElement`를 사용하며, canvas 요소는 `toDataURL` 메서드가 있으므로 올바르게 data URL을 생성함.

#### 비동기 팝아웃 복구 안전장치 추가
- **목적:** `_originalElement` 수정으로 대부분의 경우 팝아웃이 즉시 보이지만, 비동기 edge case(data URL이 큰 이미지의 지연 로드 등)에서도 복구를 보장하기 위한 방어적 코드.
- **구현:** `loadHistoryState`의 `setTimeout(renderAll, 0)` 콜백 내부에 150ms 지연 체크를 추가. 캔버스의 모든 팝아웃 객체를 순회하여, `_croppedBg`와 `linkedOriginal`이 모두 설정되어 있으나 `_element`가 아직 canvas 요소가 아닌(= `regeneratePopoutBase`가 아직 미실행) 팝아웃에 대해 `applyPopoutTilt` + `updatePopoutLine`을 재시도하고 `canvas.renderAll()`을 호출함.

#### 근본 원인 4 — 팝아웃/원본 도형에 고유 ID 미부여로 인한 연결선 끊어짐
- **문제:** `createPopoutFromObject` 함수에서 팝아웃 이미지(`img`)에 `id` 속성을 설정하지 않고, 원본 도형(`obj`)에도 `id`가 없으면 `linkedOriginalId: obj.id`가 `undefined`로 설정됨. 연결선(`line`)만 `id: 'line_' + Date.now()`으로 고유 ID가 부여되어 있었음.
- **증상:** `restorePopoutLinks`에서 `idMap`을 통해 `linkedOriginalId`로 원본 도형을 찾으려 하지만 `undefined`이므로 매칭 실패 → `linkedOriginal = null` → `updatePopoutLine`이 `!popout.linkedOriginal` 조건에 의해 early return → 연결선이 팝아웃 이동을 따라가지 않음. 마찬가지로 `linkedLineId`와 일치하는 line 객체도 `idMap`에서 찾지 못하는 경우 발생.
- **진단 방법:** `restorePopoutLinks`에 임시 `alert` 디버그 코드를 추가하여 실제 런타임에서 `id=undefined`, `origId=undefined`, `line=MISSING` 상태를 확인.
- **수정:**
  1. `createPopoutFromObject` 함수 시작부에서 원본 도형(`obj`)에 `id`가 없으면 `'orig_' + Date.now() + '_' + random`으로 자동 부여
  2. 팝아웃 이미지 `img.set({...})`에 `id: 'popout_' + Date.now() + '_' + random` 추가
  3. 이로써 `linkedOriginalId`와 `linkedLineId` 모두 유효한 값을 가지게 되어, `restorePopoutLinks`의 `idMap` 기반 참조 복원이 정상 동작

#### UI 설정 변경 내용 (팝아웃)
- **개체 기본 회전 (angle):** 기존 기본값 `8.0`에서 `6.0`으로 변경
  - 환경설정 메뉴 UI 입력란(`input#set_popout_angle`)의 기본 `value` 속성 변경
  - 값이 비어있거나 `0`일 때 강제로 기본값을 할당하는 검증 로직 변경
  - 팝아웃 개체 생성 시 환경설정 입력값이 없을 때 사용하는 Fallback 값을 `6.0`으로 변경
  - **마이그레이션 로직 추가:** `restoreSettings` 실행 시 사용자 설정에 저장된 값이 이전 기본값인 `8.0`일 경우, 새 기본값인 `6.0`으로 자동 덮어쓰도록 처리 (사용자가 별도의 초기화 과정 없이 새로운 기본값을 적용받을 수 있도록 함)

#### JSON 파일 열기 및 최근 항목 로드 시 팝아웃 연결선 끊어짐 수정
- **문제:** JSON 파일 열기( tn_action_open) 및 최근 항목 로드 경로에서 `loadFromJSON` 콜백 내에 `restorePopoutLinks()` 호출이 누락되어 있었음. `restoreLinkedTexts()`는 호출되지만 `restorePopoutLinks()`가 없어서, 팝아웃의 `linkedOriginal`, `linkedLine` 참조가 복원되지 않음.
- **증상:** JSON 파일을 열거나 최근 항목에서 로드한 후 팝아웃 개체를 이동해도 연결선이 따라가지 않음. `updatePopoutLine`이 `linkedLine` 또는 `linkedOriginal`이 null이어서 early return.
- **수정:** `loadFromJSON` 콜백 내에서 `restoreLinkedTexts()` 직후에 `restorePopoutLinks()`를 추가. 두 곳 모두 적용:
  1. JSON 파일 열기 (`file_open_input` change 이벤트 핸들러)
  2. 최근 항목 로드 (`loadRecentCapture` 함수)

#### 자르기(Crop) 후 팝아웃 연결선 끊어짐 수정 (2026-06-15)
- **문제:** 캔버스에 팝아웃 개체가 있는 상태에서 "자르기"를 통해 배경 이미지를 축소하거나 확장하면, 기존에 있던 팝아웃 개체와 원본 도형 간의 연결선(connection line)이 끊어져서 이후 팝아웃 개체를 이동해도 연결선이 더 이상 따라가지 않는 현상이 발생.
- **근본 원인:** `performEdgeCrop()` 함수(index.html L7684) 내부에서 자르기를 수행할 때 다음과 같은 타이밍 문제가 있었음:
  1. 캔버스의 모든 개체를 `canvas.remove(o)`로 일시 제거하여 배경만 남긴 뒤 새로운 크기의 배경 이미지를 설정하고, 개체를 위치 보정하여 다시 추가하는 과정을 거침.
  2. 그런데 `canvas.remove(o)` 호출 시 `object:removed` 이벤트 핸들러(L3188)가 발동됨. 이 핸들러는 원본 도형이 삭제될 때 해당 도형에 연결된 팝아웃의 `linkedOriginal`을 `null`로 설정하고 연결선(`linkedLine`)도 캔버스에서 제거하는 역할을 수행함.
  3. `isHistoryAction` 플래그가 `true`이면 이 핸들러가 조기 반환(early return)하여 연쇄 삭제를 방지하는데, `performEdgeCrop()`에서는 `isHistoryAction = true`를 개체 제거 **이후**(L7715, 콜백 내부)에 설정하고 있었음.
  4. 따라서 개체 제거 시점에 `isHistoryAction`은 `false`이므로, `object:removed` 핸들러가 정상 동작하여 팝아웃의 `linkedOriginal`을 `null`로 만들고 연결선을 삭제함.
  5. 이후 개체를 다시 추가해도 `linkedOriginal` 참조가 이미 `null`이 되어있어 복원되지 않으므로, `updatePopoutLine()`이 호출되어도 `!popout.linkedOriginal` 조건에서 조기 반환됨.
- **수정 내용 (index.html `performEdgeCrop` 함수):**
  1. **개체 제거 전에 `isHistoryAction = true` 설정:** `canvas.remove(o)` 호출 전에 `isHistoryAction` 플래그를 `true`로 설정하여, `object:removed` 핸들러가 일시 제거되는 개체들에 대해 연쇄 삭제를 수행하지 않도록 방지. (기존에 콜백 내부에서 설정하던 `isHistoryAction = true`는 제거하여 중복 설정 방지)
  2. **개체 재추가 후 `restorePopoutLinks()` 호출:** 모든 개체를 위치 보정하여 캔버스에 다시 추가한 직후, `restorePopoutLinks()` 함수를 호출하여 ID 기반으로 `linkedOriginal`과 `linkedLine` 객체 참조를 복원하고 연결선 위치를 갱신. 이는 만약 일부 참조가 누락된 경우에 대한 안전장치 역할도 수행.

### 모자이크(Mosaic) 기능 확장 및 개선 사항 (최신 변경)
- **모자이크 다각화 UI 추가**: '직사각형'으로만 지원되던 모자이크를 '타원'과 '자유선'으로도 생성할 수 있도록 하단 패널에 토글 버튼 및 선 굵기 조절 옵션을 추가했습니다.
- **자유선 모자이크 지원**: \canvas.isDrawingMode\를 활용하여 펜 도구처럼 자유롭게 선을 그려 그 영역만큼 모자이크를 적용할 수 있도록 구현했습니다.
- **마스킹 처리 고도화**: 오프스크린 캔버스와 \source-in\ 합성 연산을 통해 타원이나 자유선 형태로 자른 완벽한 모양의 모자이크 패치 이미지를 캔버스에 추가하도록 했습니다.
- **자유선 곡선 보정**: 펜(일반 곡선)과 동일하게 \getSmoothCurvePath()\를 적용하여, 자유선 모자이크 마스킹 선도 부드러운 곡선으로 자동 보정되도록 구현했습니다.
- **버그 수정**: 직사각형 및 타원 모자이크 생성 시 \setCoords()\ 호출 누락으로 인해 바운딩 박스가 제대로 계산되지 않아 모자이크가 생성되지 않던 문제를 수정했습니다.
- **자유선 곡선 보정 개선(떨림 제거)**: 자유선 모자이크 마스킹 선에 펜과 동일하게 DP 단순화(_simplifyDP) 알고리즘을 선행 적용하여 손떨림을 완벽히 제거한 후 getSmoothCurvePath()로 보정하도록 개선했습니다.
- **자유선 모자이크 선택적 곡선 보정**: 일반 펜처럼 자유선 모자이크 마스킹 중 마우스를 0.5초간 멈출 경우에만 부드러운 곡선으로 보정되도록 로직을 수정했습니다.
- **환경설정 UI 개편 및 모자이크 자유선 굵기 기본값 설정**: 환경설정의 '지우개 및 효과 설정'을 '모자이크 설정'과 '지우개 설정'으로 분리하고, 모자이크 설정 탭에 '자유선 굵기' (기본값: 30) 옵션을 추가하여 전역 기본값으로 적용 및 저장되도록 개선했습니다.
- **모자이크 설정 UI 정렬 개선**: 환경설정의 '모자이크 설정' 내 '모자이크 블록 크기'와 '자유선 굵기' 옵션이 한 줄에 나란히 표시되도록 레이아웃을 개선했습니다.
- **도형 설정 UI 정렬 개선**: 환경설정의 도형 설정 내 항목들을 공간 활용도를 높이기 위해 한 줄로 압축하여 배치했습니다.
- **저장 포맷 SVG 추가**: 환경설정의 `시스템 및 저장 설정` 내 이미지 저장 포맷 옵션에 웹 호환성이 높은 벡터 이미지 포맷인 `SVG`를 추가했습니다. 저장 로직(getSaveBlobAndName)은 이미 구현되어 있어 즉시 사용 가능합니다.

### 2026-08-25 다중 모니터 레이아웃 물리적 좌표 완벽 대응
- **물리적 모니터 배치 연동**: `mss` 모듈과 Qt `QScreen` 간의 인덱스 불일치 문제를 해결하여, 시스템의 디스플레이 설정(좌우 배치, 상하 배치, 음수 좌표 등)과 캡처 화면이 완벽하게 일치하도록 근본적인 문제를 수정했습니다.

### 2026-08-25 캔버스 가로/세로 맞춤 버튼 추가
- **가로/세로 맞춤 기능 도입**: 캔버스 좌측 하단에 '가로 길이 맞춤' 및 '세로 길이 맞춤' 아이콘 버튼을 신규 추가했습니다.
- **이미지 배율 자동 조정**: 이미지의 원래 위치나 배율에 관계없이, 버튼 클릭 시 현재 작업 영역(Workspace)의 가로 혹은 세로 길이에 맞게 캔버스 줌 배율(`zoom`)과 위치(`panX`, `panY`)가 자동으로 조정되어 화면에 꽉 차게 렌더링되도록 구현했습니다.
