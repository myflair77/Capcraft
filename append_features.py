import re

def main():
    append_text = """  26. 도형 텍스트 편집 직관성 개선, Word Wrap 및 도형 자동 조절 추가: 
      - 도형 내 글상자 클릭 시 `setCursorByClick`을 사용하여 마우스로 클릭한 정확한 위치에 즉시 커서가 배치되어 바로 수정할 수 있도록 개선했습니다.
      - 텍스트 편집 모드 진입/종료 시 도구 활성화 상태와 무관하게 텍스트 속성 토글창(크기, B, I, U, 정렬 등)이 자동으로 표시/숨김 처리되어 직관적인 편집 환경을 제공합니다.
      - 텍스트 속성 설정 변경 시 `updateActiveText`를 통해 선택된 도형에 귀속된 `linkedText`에도 실시간으로 스타일이 연동 적용되도록 수정했습니다.
      - 텍스트가 도형 너비에 도달하면 자동으로 줄바꿈(Word Wrap)이 적용되도록 `fabric.Textbox`를 적용했으며, 마름모와 타원은 내접 사각형 너비로 자동 계산하여 텍스트가 외곽선을 삐져나가지 않도록 완벽하게 처리했습니다.
      - 입력된 텍스트 양(줄 수)이 늘어나면 도형의 높이(scaleY)가 자동으로 늘어나며 텍스트를 감싸고, 텍스트를 지우면 생성 시점의 원래 크기로 자동 축소되도록 반응형 조절 로직을 도입했습니다.
"""
    with open('Capcraft_Features.md', 'a', encoding='utf-8') as f:
        f.write(append_text)

if __name__ == '__main__':
    main()
